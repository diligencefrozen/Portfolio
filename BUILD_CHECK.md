# Build Check

This project was checked after the English portfolio update.

## Verified Commands

```bash
npm ci
npm run build
npm run lint
```

## Result

- `npm ci`: passed
- `npm run build`: passed
- `npm run lint`: passed
- TypeScript compile: passed
- Vite production build: passed
- ESLint: passed

## Notes

- The portfolio copy is now written in English.
- The role is set to `Entry-Level Software Developer`.
- Education/training sections are not included.
- `public/resume.pdf` was regenerated as a one-page public resume.
- `package-lock.json` uses public `registry.npmjs.org` URLs so it can be installed outside this environment.
