# JSP Model 2 / JSTL 정리

## 1. 핵심 개념

### JSP Model 2

```
Controller = Servlet
View = JSP
Model = Java class
```

### 기본 흐름

```
사용자 요청
→ Controller(Servlet)
→ request.setAttribute()
→ index.jsp로 forward
→ index.jsp에서 jsp:include
→ 실제 JSP 화면 출력
```

### JSP에서 값 출력

```
${값}
${param.n}
${contentPage}
```

---

## 2. 페이지 이동

| 방식 | 의미 |
| --- | --- |
| redirect | 클라이언트가 새 요청 |
| forward | 서버 내부 이동, request 유지 |
| include | 현재 JSP 안에 다른 JSP 포함 |

---

## 3. Custom Tag / JSTL

### JSP 표준 액션 태그

```
<jsp:include page="second.jsp"></jsp:include>
```

### JSTL 사용 목적

```
조건문
반복문
출력
예외처리
숫자/날짜 포맷
```

---

# first.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

	<%
		// JSP Model 2
		// V(.jsp)
		//		값 받기 : EL  ${XXX}

		// 페이지 이동
		//		redirect, forward, include

		// 조건문, 반복문
		// 출력형식 (소수점, 날짜 형식, ...)

		// CustomTag
		// 	생김새는 HTML DOM 객체와 흡사.
		// .jsp => servlet으로 바뀔 때 java로 변환
		//		=> .jsp에서만 사용가능합니다.
		// <접두어:태그명>
		//		<xxx:table></xxx:table> => 열고 닫기 한 쌍으로
		// <xxx:table>(이 사이에 내용이 들어가지않는다면)</xxx:table> => <xxx:table /> 축약할수 있다!

		// JSP 표준 액션 태그
		//			JSP 환경에서 기본적으로 사용 가능
		//			그 접두어가 'jsp'
		//			forward, include, ... (해당 기능)
		// 커스텀태그
		//		외부 .jar를 넣고 사용을 함. (jstl.jar)
		//		접두어가 마음대로 지정 가능 O
		// 		반복문, 조건문, 출력 형식, ... (지정 가능)

	%>

	<h1>First</h1>
	<!-- second.jsp 소스가 아래에 !! -->
	<!-- *** 원하는 자리에 넣어서 표현할 수 있다는 게 장점 -->
	<jsp:include page="second.jsp"></jsp:include>
	<h1>Third</h1>

</body>
</html>
```

---

# second.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<h1>Second</h1>

</body>
</html>
```

---

# AController.java

## 역할

`AController` 요청 시 `a.jsp`를 `index.jsp` 안에 포함한다.

```java
package com.beaver.may082.a;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/AController")
public class AController extends HttpServlet {
	private static final long serialVersionUID = 1L;

    public AController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		request.setAttribute("contentPage", "a.jsp");
		request.getRequestDispatcher("index.jsp").forward(request, response);
	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}
```

---

# HomeController.java

## 역할

`HomeController` 요청 시 `home.jsp`를 `index.jsp` 안에 포함한다.

```java
package com.beaver.may082.main;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/HomeController")
public class HomeController extends HttpServlet {
	private static final long serialVersionUID = 1L;

    public HomeController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		request.setAttribute("contentPage", "home.jsp");
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}
```

---

# HomeController.java 다른 버전

## 차이

`index.jsp`가 아니라 `index1.jsp`로 이동한다.

`doPost()` 요청도 `doGet()`으로 처리한다.

```java
package com.beaver.may082.main;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.*;

@WebServlet("/HomeController")
public class HomeController extends HttpServlet {

    private static final long serialVersionUID = 1L;

    public HomeController() {
        super();
    }

    protected void doGet(HttpServletRequest request,
                         HttpServletResponse response)
            throws ServletException, IOException {

        request.setAttribute("contentPage", "home.jsp");

        request.getRequestDispatcher("index1.jsp")
               .forward(request, response);
    }

    protected void doPost(HttpServletRequest request,
                          HttpServletResponse response)
            throws ServletException, IOException {

        doGet(request, response);
    }
}
```

---

# calc/input.jsp

## 역할

`x`, `y` 값을 입력받아 `CalcController`로 전송한다.

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<form action="CalcController" name="calcForm" onsubmit="return calcCheck();">
		<table id= "calcTbl">
			<tr>
				<td>
					x : <input placeholder="x" autofocus="autofocus" autocomplete="off" name="x">
				</td>
			</tr>

			<tr>
				<td>
					y : <input placeholder="y" autocomplete="off" name="y">
				</td>
			</tr>

			<tr>
				<td align="center">
					<button>계산</button>
				</td>
			</tr>

			</table>
	</form>

</body>
</html>
```

---

# BeaverValidChecker.js

## 핵심 규칙

```
문제가 있으면 true
정상이면 false
```

```jsx
/**
 *
 */
// 잘못됐으면 true값
//(뭔가 문제가 되면 true, 정상적이면 false)

// <input> 을 넣었을 때
// 아무것도 안썼으면 true, 뭐라도 썼으면 false
function isEmpty(input) {
	return (!input.value);

}

// <input>을 넣었을떄
// 한글/ 특수문자 적혀있다면 true, 없다면 false
function containsAnother(input) {
	let pass = "1234567890zxcvbnmasdfghjklqwertyuiopZXCVBNMASDFGHJKLQWERTYUIOP@_.";
	let iv = input.value;
	for (let i = 0; i < iv.length; i++) {
		if (pass.indexOf(iv[i]) == -1) { // pass 에 있는 내용중에서 내가 쓴 내용이 없으면

			return true;
			}
	}
	return false;
}

// <input>, 글자 수를 넣었을 때
// 그 글자 수 보다 적으면 true, 그 글자수 이상이면 false
function atLeastLetter(input, len) {
	return (input.value.length < len);
}

// <input> x 2 넣었을 때
// 내용이 다르면 true, 같으면 false (pw, pw 확인)
function notEqualPw(input1, input2) {
	return (input1.value != input2.value);
}

// <input>, 문자열세트 넣었을 때
// 없으면 true, 있으면 false, (입력한 pw에 지정한 문자열이 없는 경우)
function notContain(input, passSet) {
	let iv = input.value;
	for (let i = 0; i < passSet.length; i++) {
		if (iv.indexOf(passSet[i]) != -1) {
			return false;
		}
	}
	return true;
}

// <input> 넣었을 때
//숫자 아닌거 있으면 true, 숫자만 있으면 false
function isNotNumber(input) {
	return isNaN(input.value);
}

// <input>, 확장자 넣었을 때
// 확장자명이 아니면 true, 맞으면 false
function isNotType(input, type) {
	// .png, .gif, .bmp, ...
	type = "." + type;
	return (input.value.indexOf(type) == -1);
}
```

---

## JS 함수 요약

| 함수 | true 의미 |
| --- | --- |
| `isEmpty()` | 비어 있음 |
| `containsAnother()` | 허용 문자 외 문자가 있음 |
| `atLeastLetter()` | 글자 수 부족 |
| `notEqualPw()` | 두 값이 다름 |
| `notContain()` | 지정 문자가 없음 |
| `isNotNumber()` | 숫자가 아님 |
| `isNotType()` | 지정 확장자가 아님 |

---

# CalcController.java

## 역할

처음 접속이면 입력 화면, 파라미터가 있으면 계산 결과 화면으로 이동한다.

```java
package com.beaver.may083.calc;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/CalcController")
public class CalcController extends HttpServlet {
	private static final long serialVersionUID = 1L;

    public CalcController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		if (!request.getParameterNames().hasMoreElements()) {
			request.setAttribute("contentPage", "calc/input.jsp");
		} else {
			Calculator.calculate(request);
			request.setAttribute("contentPage", "calc/result.jsp");
		}
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}
```

---

## 흐름

```
/CalcController
→ 파라미터 없음: calc/input.jsp
→ 파라미터 있음: Calculator.calculate(request) 후 calc/result.jsp
```

---

# jstlc/input.jsp

## 역할

숫자 `n`을 입력받아 `JSTLController`로 전송한다.

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

	<form action="JSTLController">
		숫자 : <input name="n" autocomplete="off"> <p>
		<button>입력</button>
	</form>

</body>
</html>
```

---

# jstlc/output.jsp

## JSTL core 선언

```
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
```

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<h1>${param.n }</h1>

	<!-- 조건문 -->
	<c:if test="${param.n % 2 == 0 }">
		짝수!!
	</c:if>
	<hr>
	<!-- if-else + switch-case 느낌 -->
	<c:choose>
		<c:when test="${param.n > 10 }">
			10보다 크다
		</c:when>

		<c:when test="${param.n >= 5 }">
			5 ~ 10 사이
		</c:when>
		<c:otherwise>
			나머지
		</c:otherwise>
	</c:choose>

	<hr>

	<%
		// for (int i = 1; i <= 5; i +=2) {
		//		System.out.println(i);
		//}
	%>
	<!-- 반복문 -->
	<c:forEach var="v" begin="1" end='${param.n }' step="2">
		<marquee>${v }</marquee>
	</c:forEach>
	<hr>

	<%
		// for (int i : ar) {
		//		System.out.println(i);
		//}

	%>

	<c:forEach var="i" items="${ar }">
		<marquee>${i }</marquee>
	</c:forEach>

	<c:forEach var="i" items="${al }">
		${beaver.name } - ${beaver.age }<br>
	</c:forEach>
	<hr>

	<!-- try-catch -->
	<c:catch var="myExcept">
		<% int result = 100 / 2; %>
		계산 결과 : <%=result %>
	</c:catch>
	<br>
	<c:if test="${myExcept != null }">
		에러 발생 : ${myExcept.message }
	</c:if>

	<br>
	<c:out value="${param.n } zzzzzzzzzzz"></c:out> <!-- 출력 -->
	<c:set var = "name" value = "홍길동" /> <!-- 변수 만들기 -->
	<c:out value="${name }" />

	<!--  StringTokenizer -->
	<c:forTokens var="items" items = "서울,대전,대구,부산,찍고,아하" delims=",">
		지역 : ${item } <br>
	</c:forTokens>

</body>
</html>
```

---

## JSTL core 태그 요약

| 태그 | 의미 |
| --- | --- |
| `<c:if>` | if |
| `<c:choose>` | if-else / switch |
| `<c:when>` | 조건 분기 |
| `<c:otherwise>` | 나머지 |
| `<c:forEach>` | 반복문 |
| `<c:catch>` | 예외 처리 |
| `<c:out>` | 출력 |
| `<c:set>` | 변수 생성 |
| `<c:forTokens>` | 문자열 분리 |

---

## 주의 포인트

```
<c:forEach var="i" items="${al }">
	${beaver.name } - ${beaver.age }<br>
</c:forEach>
```

일반적으로는 `var="i"`를 사용했으므로 아래처럼 쓰는 것이 자연스럽다.

```
${i.name} - ${i.age}
```

또한 아래 코드도 변수명이 다르다.

```
<c:forTokens var="items" ...>
	지역 : ${item}
</c:forTokens>
```

일반적으로는 아래처럼 맞춘다.

```
${items}
```

---

# JSTLController.java

## 역할

파라미터가 없으면 입력 페이지, 있으면 데이터를 만들고 페이지 이동한다.

```java
package com.beaver.may083.jstlc;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/JSTLController")
public class JSTLController extends HttpServlet {
	private static final long serialVersionUID = 1L;

    public JSTLController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		if (!request.getParameterNames().hasMoreElements()) {
			request.setAttribute("contentPage", "jstlc/input.jsp");
		} else {
			JSTLCDataMaker.make(request);
			request.setAttribute("contentPage", "jstlc/input.jsp");
		}
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}
```

---

## 주의 포인트

현재 else에서도 `jstlc/input.jsp`로 이동한다.

```java
request.setAttribute("contentPage", "jstlc/input.jsp");
```

출력 페이지를 보려면 보통 아래처럼 쓴다.

```java
request.setAttribute("contentPage", "jstlc/output.jsp");
```

---

# index.jsp

## 역할

공통 레이아웃 페이지.

Controller가 넘긴 `contentPage`를 include 한다.

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Index</title>

<style>
	nav ul {
		list-style: none;
		padding: 0;
		display: flex;
		justify-content: center;
		gap: 20px;
		margin: 0;
		background-color: #333;

	}

	nav li {
		border-right: 1px solid #444;

	}

	nav li:last-child {
		border-right: none;

	}

	nav a {
		text-decoration: none;
		color: #fefefe;
		display: block;
		padding: 14px 25px;
		transition: background-color 0.3s;

	}

	nav a:hover {
		background-color: #111;
		color: #227799;
	}

	h1 {
		display: flex;
		justify-content: center;

	}

	#main {
		font-weight: bold;
		font-style: inherit;
		left:20px;
		margin: 0;

	}

	.button {
		display: flex;
		justify-content: center;
		align-items: center;
		text-align: center;

	}

	footer {
		display: flex;
		justify-content: center;
		text-align: center;
		background-color: #999999;
		flex-direction: column;
		align-items: center;
		padding: 20px;
		gap: 10px;

	}

	footer nav a {
		text-decoration: none;
		margin: 0 10px;
	}

	.pepe {
		display: flex;
		justify-content: center;
		align-items: center;

	}

	#AController {
		display: block;
	}

	#AController:has(~ .content:target) {
		display: none;
	}

	.content {
		display: none;
	}

	.content:target {
		display: block;
	}

</style>
</head>
<body>

	<h1 id="main">2026년 05월 08일</h1>
	<div class="pepe">
		<img alt="PEPE" src="img/pepe.gif">
	</div>
	<nav>
		<ul>
		<li><a href="#HomeController">Home</a></li>
		<li><a href="#AController">A</a></li>
		<li><a href="#BCOntroller">B</a></li>
		<li><a href="#CController">C</a></li>
		</ul>
	</nav>

	<h1 id="AController">A</h1>
	<div>
			<jsp:include page="${contentPage }"></jsp:include>
		<!-- 이 부분을 실행하려면, HomeController 쪽에서 실행을 시도해야 오류가 안 남. -->
	</div>

	<h1 id="BCOntroller">B</h1>
	<div>
		<jsp:include page="${contentPage }"></jsp:include>
	</div>

	<h1 id="CController">C</h1>

	<footer>
		<p>&copy: 2026 SOLDESK. All tights reserved.</p>
		<nav>
			<a href="">Privacy Policy</a> |
			<a href="">Terms of Service</a>
		</nav>
		<address>
			Contact us : <a href="">test123@gmail.com</a>
		</address>
	</footer>

</body>
</html>
```

---

## index.jsp 주의 포인트

| 코드 | 의미 |
| --- | --- |
| `<jsp:include page="${contentPage }">` | Controller가 정한 JSP 포함 |
| `href="#HomeController"` | Controller 요청이 아니라 내부 앵커 |
| `href="HomeController"` | Controller로 요청 |
| include가 두 번 있음 | 같은 contentPage가 두 번 나올 수 있음 |

---

# JSTLCDataMaker.java

## 역할

JSTL 반복문 예제용 데이터를 request에 저장한다.

```java
package com.beaver.may083.jstlc;

import java.util.ArrayList;

import javax.servlet.http.HttpServletRequest;

import com.beaver.may083.beaver.Beaver;

public class JSTLCDataMaker {
	public static void make(HttpServletRequest req) {
		int[] ar = { 123, 456, 78, 91, 234 };
		req.setAttribute("ar", ar);

		ArrayList<Beaver> al = new ArrayList<Beaver>();
		al.add(new Beaver("비버", 3, 50.5, 10.1));
		al.add(new Beaver("빕어", 2, 20.5, 13.1));
		al.add(new Beaver("비붜", 5, 30.5, 12.1));
		al.add(new Beaver("빕워", 1, 10.5, 11.1));
		req.setAttribute("al", al);
	}

}
```

---

# Beaver.java

## 역할

JavaBean 클래스.

EL은 getter를 통해 값에 접근한다.

```java
package com.beaver.may083.beaver;

public class Beaver {
	private String name;
	private int age;
	private double height;
	private double weight;
	public Beaver(String name, int age, double height, double weight) {
		super();
		this.name = name;
		this.age = age;
		this.height = height;
		this.weight = weight;
	}
	public String getName() {
		return name;
	}
	public void setName(String name) {
		this.name = name;
	}
	public int getAge() {
		return age;
	}
	public void setAge(int age) {
		this.age = age;
	}
	public double getHeight() {
		return height;
	}
	public void setHeight(double height) {
		this.height = height;
	}
	public double getWeight() {
		return weight;
	}
	public void setWeight(double weight) {
		this.weight = weight;
	}

}
```

---

## JavaBean 암기

```
private 필드
생성자
getter / setter
EL에서는 ${객체명.필드명} 형태로 접근
```

예:

```
${beaver.name}
```

실제로는 `getName()`을 통해 접근한다고 이해하면 된다.

---

# JSTLFDataMaker.java

## 역할

JSTL fmt 예제용 데이터를 만든다.

```java
package com.beaver.may083.jstlf;

import java.util.Date;

import javax.servlet.http.HttpServletRequest;

public class JSTLFDataMaker {
	public static void make(HttpServletRequest req) {
		int a = 123456789;
		req.setAttribute("a", a);

		double b = 10 / 24.0;
		req.setAttribute("b", b);

		double c = 123.456789;
		req.setAttribute("c", c);

		Date now = new Date();
		req.setAttribute("d", now);
	}

}
```

---

## 저장 데이터

| 이름 | 용도 |
| --- | --- |
| `a` | 숫자/통화 |
| `b` | 퍼센트 |
| `c` | 소수점 |
| `d` | 날짜/시간 |

---

# jstlf/output.jsp

## JSTL fmt 선언

```
<%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %>
```

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!-- core 가 있어야 fmt를 쓸수 있다 (x)
	core / fmt 자주 쓰는 기능이라 보통은 둘 다 가져다 씀 (O) -->
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<!--  fmt : 숫자 / 날짜 형태 포맷팅 (formatting) -->
	<fmt:formatNumber value="${a }" type="number" /> <!--  숫자 -->
	<fmt:formatNumber value="${a }" type="currency" /> <!-- 국가에 맞는 화폐단위 -->
	<fmt:formatNumber value="${a }" type="currency" currencySymbol="$"/> <!-- 통화 -->
	<!--  퍼센트 (소수점 반올림) -->
	<fmt:formatNumber value="${b } " type="percent"/>
	<!-- 소수점 둘쨰자리까지 (빤올림) -->
	<fmt:formatNumber  value="${c}" pattern="#.##"/>
	<!--
		Date 속성
		: type = "date / time / both"
			1.date
				dateStyle ="short / long"

			2. time
				timeStyle = "short / long"

			3. both
				dateStyle = "short / long"
				timeStyle = "short / long"
	 -->

	 <fmt:formatDate value="${d}" type="both" dateStyle="long" timeStyle="short"/><br>
	 <!-- 패턴 : SimpleDateFormat의 패턴과 동일 -->
	 <fmt:formatDate value="${d}" pattern="yyyy-MM-dd E a hh"/><br>
</body>
</html>
```

---

## fmt 태그 요약

| 태그 | 의미 |
| --- | --- |
| `<fmt:formatNumber>` | 숫자 형식 변경 |
| `type="number"` | 일반 숫자 |
| `type="currency"` | 통화 |
| `currencySymbol="$"` | 통화 기호 지정 |
| `type="percent"` | 퍼센트 |
| `pattern="#.##"` | 소수점 둘째 자리 |
| `<fmt:formatDate>` | 날짜 형식 변경 |
| `type="date"` | 날짜 |
| `type="time"` | 시간 |
| `type="both"` | 날짜 + 시간 |
| `pattern` | 직접 형식 지정 |

---

# JSTLFController.java

## 역할

fmt 예제 데이터를 만들고 `jstlf/output.jsp`로 보낸다.

```java
package com.beaver.may083.jstlf;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/JSTLFController")
public class JSTLFController extends HttpServlet {
	private static final long serialVersionUID = 1L;

    public JSTLFController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		JSTLFDataMaker.make(request);
		request.setAttribute("contentPage", "jstlf/output.jsp");
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}
```

---

# 전체 흐름 요약

## Controller 공통 패턴

```
1. Controller 요청
2. request.setAttribute("contentPage", "실제페이지.jsp")
3. index.jsp로 forward
4. index.jsp에서 jsp:include
5. 실제 JSP 출력
```

---

## 핵심 코드

```java
request.setAttribute("contentPage", "페이지.jsp");
request.getRequestDispatcher("index.jsp").forward(request, response);
```

```
<jsp:include page="${contentPage }"></jsp:include>
```

---

# 시험 준비 암기용 정리

## 1. Model 2

```
Servlet = Controller
JSP = View
Java class = Model
```

---

## 2. request 관련

| 코드 | 의미 |
| --- | --- |
| `request.getParameter("x")` | 사용자가 보낸 값 받기 |
| `request.setAttribute("name", value)` | JSP에 보낼 값 저장 |
| `${param.n}` | 요청 파라미터 출력 |
| `${name}` | request 등에 저장된 속성 출력 |

---

## 3. forward / include

| 개념 | 의미 |
| --- | --- |
| forward | 서버 내부 페이지 이동 |
| include | 현재 JSP 안에 다른 JSP 삽입 |
| redirect | 새 요청 발생 |

---

## 4. JSTL core

```
<c:if> = if
<c:choose> = if-else / switch
<c:when> = 조건
<c:otherwise> = 나머지
<c:forEach> = 반복문
<c:catch> = try-catch
<c:out> = 출력
<c:set> = 변수 생성
<c:forTokens> = 문자열 분리
```

---

## 5. JSTL fmt

```
<fmt:formatNumber> = 숫자 포맷
<fmt:formatDate> = 날짜 포맷
type="number" = 숫자
type="currency" = 통화
type="percent" = 퍼센트
pattern = 직접 형식 지정
```

---

## 6. JavaBean

```
private 필드
getter / setter
EL은 getter를 통해 접근
${beaver.name} → getName()
```

---

## 7. 자주 나오는 시험 문장

```
JSP Model 2는 Servlet이 요청을 제어하고 JSP가 화면을 출력하는 MVC 방식이다.
Controller는 request에 데이터를 저장하고 forward로 JSP에 전달한다.
index.jsp는 공통 레이아웃 역할을 하고 contentPage를 include한다.
JSTL core는 조건문과 반복문, fmt는 숫자와 날짜 포맷에 사용한다.
```

---

## 8. 오류 포인트 암기

| 코드 | 주의점 |
| --- | --- |
| `${contentPage}` | 값이 없으면 include 오류 가능 |
| `href="#HomeController"` | Controller 요청 아님, 내부 앵커 |
| `href="HomeController"` | Controller 요청 |
| `var="i"`인데 `${beaver.name}` | 보통 `${i.name}`이 맞음 |
| `var="items"`인데 `${item}` | 보통 `${items}`가 맞음 |
| `jstlc/input.jsp` | 출력 보려면 보통 `jstlc/output.jsp` |

---

## 9. 암기

```
@WebServlet = URL 매핑
doGet = GET 요청 처리
setAttribute = JSP로 값 전달
forward = request 유지 이동
jsp:include = JSP 포함
EL = ${}
c = JSTL core
fmt = JSTL formatting
JavaBean = getter/setter
```

---
