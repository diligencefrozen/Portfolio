# FirstC.java

```java
package com.beaver.may131.main;

import java.io.IOException;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

// Controller
//	JSP 환경에서 상황 판단을..시도
//	(GET? POST?) GET인지 POST인지 판단하기에는 Servlet이 적합.
// 		계산/ 로직이 필요하다면 M(Model)으로
//		보여줘야하면 V(View)로..
// 흐름 제어용으로 용이.

@WebServlet("/FirstC") // <= FirstC에 Tomcat을 등록
public class FirstC extends HttpServlet {
	private static final long serialVersionUID = 1L;
	
	// 이 파일에는 main메소드는 없음.
	// 톰캣이 실행(main 기능은 tomcat에 있음)
	//		=> 알아서 FirstC 객체를 만들고..1
       
	// 이 프로젝트가 처음 시작할 때  기본 생성자 하나를 생성
    public FirstC() {
        super();
        // TODO Auto-generated constructor stub
        // 처음 실행했을 때 사과 데이터 총 갯수를 받아오기 위해서 사용을 했었음. (이전 프로젝트 기준)
    }

    // 웹사이트의 페이지 전환 :
    // C로 요청 => M애 가서 작업 => V로 포워딩
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		// 사이트에 처음 들어오면 (GET 방식)
		// FirstC로 요청파라미터 하나도 없이 GET 방식으로 요청을 하면 아래의 IF 문이 작동
		if (!request.getParameterNames().hasMoreElements()) {
			
			// firstV.jsp 로 지금 일어난 요청을 제보해주는 객체
			RequestDispatcher rd = request.getRequestDispatcher("firstV.jsp");
			rd.forward(request, response); //firstV.jsp 로 포워딩
		}

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}

```

# firstV.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<script type="text/javascript">
	function gogogo() {
		location.href = "SecondC?a=20&b=200";
	}
</script>
</head>
<body>
	<%
		// 사용자가 어떠한 액션을 취했을 때 이동 : 요청
		// - <a> 태그를 써서 : 클릭하면 GET 방식.
		// - JS를 써서 요청 : 이벤트를 추가 했을 떄 GET 방식.
		//		(JS로 HTML만드는게 가능 => JS로 <form> 을 만들면 POST 방식도 가능 O)
		// - <form> + <button> : 버튼을 클릭했을 때 GET/POST 이냐를 판단하는 방식
		
	%>
	<h1>First</h1>
	d : ${sessionScope.d } <br>
	e : ${cookie.e.value } <br>
	<a href="SecondC?a=10&b=100">SecontC로</a>
	<hr>
	<div onclick="gogogo();">SecondC로</div>
	<hr>
	<form action="SecondC">
		<input name="a"> <br>
		<input name="b"> <br>
		<button>SecondC</button>
	</form>

</body>
</html>
```

# SecondC.java

```java
package com.beaver.may131.main;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/SecondC")
public class SecondC extends HttpServlet {
	private static final long serialVersionUID = 1L;
       

    public SecondC() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		M.makeSecondData(request, response);
		M.printSecondData(request);
		M.printSecondDataNew(request);
		request.getRequestDispatcher("secondV.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}

```

# M.java

```java
package com.beaver.may131.main;

import javax.servlet.http.Cookie;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

// 값
//		갈색요청 소속
//		parameter(a, b)
//		attribute(c)
//		갈색이 닿으면 해당 값을 사용 가능 O
//		새로운 요청이 일어나는 상황에서는 값 사용 불가...!
//		Third까지 살아서 갈 수 있는 방법이 필요
//		<현재 thirdV 쪽으로는 갈색 요청이 닿지 않음>

// 		- parameter
//			GET 방식 : 주소에(url?파라미터명=값&파라미터=값&...)
//			POST 방식 : 내부전달(주소에 url?파라미터명=값 안보임)

//			V에서 만든 값 
//			String or String[]
//			request 소속

//`````````값 읽기
//				JAVA : request.getParameter("파라미터명");
//				JSP (EL) : ${param.파라미터명 }

//		- attribute
//				M에서 만든 값
//				Object
//				request 소속

// 값 읽기
//		JAVA : request.getAttribute("어트리뷰트 명");
//		JSP(EL) : ${어트리뷰트명 }																																																																																					//
///////////////////
// HTTP 프로토콜 특성이자 약점
// 1. Connectionless
//		사용자가 요청을 한 후에 응답받았을 때 연결이 끊기는 특징
// 2. Stateless
//		통신이 끝나면 상태를 끊어버리는 특징

// 이 약점을 보완하기 위해서 쿠키 / 세션 사용

// 쿠키 (Cookie) :
//				사용자의 상태 정보를 컴퓨터에 저장을 했다가 나중에 참조
// 				사용자가 따로 요청을 안해도 브라우저가 요청을 하면
//				자동으로 서버에 전송을 해줌
//				한 번 시간을 정해놓으면 브라우저의 종료 여부와 상관없이
//				유지할 수 있음.
//				=> ID/PW 저장하기, 쇼핑몰 장바구니, 팝업창 (오늘 하루 보지않기), ...

// M에서 만든 값
// String
// 기본 유지시간 - 24시간 (쿠키)
// 사용자 컴퓨터에 저장되는데..
//		- 요새는 암호화 해서 저장한다지만... 상당히 위험함!
//			- 노출되면 위험한 정보는 쿠키에 저장하지마세요 X

// 값 읽기
//		Java : 현재 사용자 컴퓨터에 만들어져 있는 쿠키들에 접근
//				=> 그 중에 이 프로젝트에 있는 쿠키를 찾기
//		JSP(EL) : ${cookie.이름.value }

// 세션 (Session) :
//					쿠키를 기반으로 동작 하지만, 사용자의 정보를 Server애서 관리함.
//					접속시간 설정 O, 일정 시간동안 응답이 없으면 정보를 삭제
//					쿠키보다는 보안이 좋음, 메모리 사용량 큼.
//					동접자 수 많으면 => 서버 과부하됨! => 성능 저하!

// 					session 소속 attribute
//					M에서 만든 값
//					Object
//					연결만 되어있으면 어디서든지 사용이 가능
//					유지시간 : 기본 30분, 설정 가능 O
//						시간동안 아무 액션이 없으면 자동으로 끊김.

// 쿠키 : 정해놓은 시간이 만료되거나, 쿠키를 삭제하기 전까지는 유지.
// 세션 : 만료되는 시간을 지정할수는 있지만, 브라우저가 종료되면 바로 삭제.

public class M {
	public static void printSecondData(HttpServletRequest req) {
		int a = Integer.parseInt(req.getParameter("a"));
		int b = Integer.parseInt(req.getParameter("b"));
		int c = (Integer) req.getAttribute("cc");
		
		System.out.println(a);
		System.out.println(b);
		System.out.println(c);
	}
	
	public static void makeSecondData(HttpServletRequest req, HttpServletResponse res) {
		int c = 3000;
		req.setAttribute("cc", c);
		
		int d = 4000;
		HttpSession hs = req.getSession(); // 사용자와 서버간의 연결상태
		hs.setMaxInactiveInterval(15); // session 의 유지시간 15초
		hs.setAttribute("d", d); // session 소속의ㅏ attribute
		
		String e = "500000";
		Cookie ck = new Cookie("e", e);
		ck.setMaxAge(15); // 15초 동안은 사용자 컴퓨터에 파일로 저장
		res.addCookie(ck); // 응답객체에 쿠키를 추가
	}
	
	public static void printSecondDataNew(HttpServletRequest req) {
		HttpSession hs = req.getSession();
		int d = (Integer) hs.getAttribute("d");
		System.out.println(d);
		
		// 이 프로젝트, naver, youtube, ...
		// 현재 사용자 컴퓨터에 있는 쿠키들...
		Cookie[] cks = req.getCookies(); 
		// 저장되어있는 쿠키 '들' 이라서 배열
		for (Cookie ck : cks) {
			if (ck.getName().equals("e")) { // 쿠키이름 e인 값을 찾아라
				System.out.println(ck.getValue()); // 출력
			}
		}
	}

}

```

# secondV.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<h1>Second</h1>
	<a href="ThirdC">ThirdC로</a>
	<hr>
	a : ${param.a }<br>
	b : ${param.b }<br>
	c : ${cc }<br>
	d : ${sessionScope.d } <br>
	e : ${cookie.e.value } <br>
	
	<hr>
	<%
		// 실행했을 때 무한루프..
		// : include 가 잘못됐을 때 or 없는 페이지를 include 했을 때
	%>
	<jsp:include page="${baby }"></jsp:include>

</body>
</html>
```

# ThirdC.java

```java
package com.beaver.may131.main;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/ThirdC")
public class ThirdC extends HttpServlet {
	private static final long serialVersionUID = 1L;
       

    public ThirdC() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		request.setAttribute("baby",  "thirdVbaby.jsp");
		request.getRequestDispatcher("thirdV.jsp").forward(request, response);
		
		

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}

```

# thirdV.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<h1>Third</h1>
	<hr>
	a : ${param.a }<br>
	b : ${param.b }<br>
	c : ${cc }<br>
	d : ${sessionScope.d } <br>
	e : ${cookie.e.value } <br>
	
	<hr>

</body>
</html>
```

# thirdVbaby.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<h1>ThirdVBaby</h1>
	<hr>
	a : ${param.a }<br>
	b : ${param.b }<br>
	c : ${cc }<br>
	d : ${sessionScope.d } <br>
	e : ${cookie.e.value } <br>
	<hr>

</body>
</html>
```

---

# HomeController.java

```java
package com.beaver.may132.main;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.beaver.may132.member.MemberDAO;

@WebServlet("/HomeController")
public class HomeController extends HttpServlet {
	private static final long serialVersionUID = 1L;
       

    public HomeController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		if (MemberDAO.loginCheck(request)) {
			request.getRequestDispatcher("loginSuccess.jsp").forward(request, response);
		} else {
			request.getRequestDispatcher("index.jsp").forward(request, response);

			
		}
		
		
		
		
		
		
	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}

```

# index.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<form action="LoginController" method="post">
		ID <input name="id" value='${cookie.lastLoginId.value }'> <p>   
		PW <input name="pw"> <p>
		<button>로그인</button>
	</form>

</body>
</html>
```

# MemberDAO.java

```java
package com.beaver.may132.member;

import javax.servlet.http.Cookie;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class MemberDAO {
	
	// 페이지 여기저기 이동할 건데..
	// => 로그인이 되어있는지 확인이 되어야겠음..!
	public static boolean loginCheck(HttpServletRequest req) {
		Member m = (Member) req.getSession().getAttribute("m");
		// 로그인이 유지중이거나 로그인에 성공을 했으면
		//		12번줄의 값이 존재할 것 !
		return (m!=null);
	}
	
	
	// 로그인 기능
	public static  void login(HttpServletRequest req, HttpServletResponse res) {
		try {
			req.setCharacterEncoding("UTF-8");
			String id = req.getParameter("id");
			String pw = req.getParameter("pw");
			
			// 로그인 성공한 유저 정보
			//		사이트 어디서든지 사용 가능
			//		일정 시간이 지나면 로그인 상태 풀리게
			//		session 소숙의 attribute or cookie
			// 		cookie는 보안상 위험함.
			
			if (id.equals("헬프") && pw.equals("me")) {
				Member m = new Member(id, pw);
				req.getSession().setAttribute("m",  m);
				req.getSession().setMaxInactiveInterval(10);
				
				// 한번 로그인에 성공했으면
				// 다음에 이 사이트에 들어왔으따,
				// <input> 에 id가 남아있었으면
				// 접속을 끊어도, 재부팅을 해도 남아있었으면
				// ... => cookie!
				Cookie c = new Cookie("lastLoginid", id);
				c.setMaxAge(600);
				res.addCookie(c);
				
			}
		} catch (Exception e) {
			// TODO: handle exception
		}
	}

}

```

# Member.java

```java
package com.beaver.may132.member;

public class Member {
	private String id;
	private String pw;
	
	
	public Member() {

	}
	
	public Member(String id, String pw) {
		super();
		this.id = id;
		this.pw = pw;
	}

	public String getId() {
		return id;
	}

	public void setId(String id) {
		this.id = id;
	}

	public String getPw() {
		return pw;
	}

	public void setPw(String pw) {
		this.pw = pw;
	}
	
	
	
	

}

```

# loginSuccess.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<h1>환영합니다</h1>
	<a href="HomeController">로그인 유지 테스트</a>

</body>
</html>
```

# LoginController.java

```java
package com.beaver.may132.member;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/LoginController")
public class LoginController extends HttpServlet {
	private static final long serialVersionUID = 1L;
       

    public LoginController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		MemberDAO.login(request, response);
		// 1. 로그인 성공시 => loginSuccess.jsp
		// 2. 로그인 실패시 => index.jsp
		if (MemberDAO.loginCheck(request)) {
			request.getRequestDispatcher("loginSuccess.jsp").forward(request, response);
		} else {
			request.getRequestDispatcher("index.jsp").forward(request, response);
			
		}
		
	}
	
}

```

---

<aside>
💡

[회원]

1. 회원가입 + 유효성 검사
2. 로그인 / 로그아웃
3. 회원정보 수정
4. 회원 탈퇴

---

[게시글]

1. 게시글 작성
2. 게시글 조회 ( + 페이징 처리 )
3. 게시글 수정 / 삭제 (해당 회원이 작성한 글에 대해서만 접근 가능)
    
                                      ( 회원이 탈퇴하면 그 회원이 작성한 게시글도 삭제)
    
4. 게시글 조건 검색 (+ 페이징 처리)
    
    (게시글 내용을 검색하면 그 내용을 포함하고 있는 게시글 조회)
    
</aside>

# HomeController.java

src/main/java/com/jeesung/may131/main/HomeController.java

```java
package com.jeesung.may131.main;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/HomeController")
public class HomeController extends HttpServlet {
	private static final long serialVersionUID = 1L;

	public HomeController() {
		super();
	}

	protected void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		request.setCharacterEncoding("UTF-8");

		String page = request.getParameter("page");
		String contentPage = "home.jsp";

		if (page == null || page.equals("home")) {
			contentPage = "home.jsp";
		} else if (page.equals("board")) {
			contentPage = "board.jsp";
		} else if (page.equals("join")) {
			contentPage = "join.jsp";
		} else if (page.equals("login")) {
			contentPage = "login.jsp";
		} else if (page.equals("memberEdit")) {
			contentPage = "memberEdit.jsp";
		} else if (page.equals("boardWrite")) {
			contentPage = "boardWrite.jsp";
		} else if (page.equals("boardDetail")) {
			contentPage = "boardDetail.jsp";
		}

		request.setAttribute("cp", contentPage);
		request.getRequestDispatcher("/WEB-INF/views/index.jsp").forward(request, response);
	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		doGet(request, response);
	}
}
```

# index.jsp

src/main/webapp/WEB-INF/views/index.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>

<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>게시판</title>

<link rel="stylesheet" href="${pageContext.request.contextPath}/css/index.css">
<script type="text/javascript" src="${pageContext.request.contextPath}/js/go.js"></script>
<script type="text/javascript" src="${pageContext.request.contextPath}/js/validChecker.js"></script>
</head>

<body>

<div class="app">

	<header class="topbar">
		<div class="brand" onclick="goPage('home');">
			<div class="brand-mark">B</div>
			<div>
				<h1>게시판</h1>
				<p>안녕하세요? 게시판 입니다.</p>
			</div>
		</div>

		<nav class="nav">
			<a href="HomeController?page=home">메인화면</a>
			<a href="HomeController?page=board">게시판</a>
			<a href="HomeController?page=join">회원가입</a>
			<a href="HomeController?page=login">로그인</a>
		</nav>
	</header>

	<main class="main">
		<c:if test="${not empty cp}">
			<jsp:include page="${cp}" />
		</c:if>
	</main>

	<footer class="footer">
		<p>Built</p>
	</footer>

</div>

</body>
</html>
```

# home.jsp

src/main/webapp/WEB-INF/views/home.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>

<section class="hero">
	<div class="hero-copy">
		<p class="eyebrow">메인화면</p>
		
		<p class="hero-desc">

		</p>

		<div class="hero-actions">
			<button class="btn primary" onclick="goPage('board');">게시글 탐색</button>
			<button class="btn ghost" onclick="goPage('join');">회원가입</button>
		</div>
	</div>

	<div class="hero-card">
		<div class="status-dot"></div>
		<h3>테스트</h3>
		<ul>
			<li>1</li>
			<li>2</li>
			<li>3</li>
			<li>4</li>
		</ul>
	</div>
</section>

<section class="feature-grid">
	<div class="feature-card">
		<span>01</span>
		<h3>Member</h3>
		<p>테스트1</p>
	</div>

	<div class="feature-card">
		<span>02</span>
		<h3>Board</h3>
		<p>테스트2</p>
	</div>

	<div class="feature-card">
		<span>03</span>
		<h3>test</h3>
		<p>테스트3</p>
	</div>
</section>
```

# join.jsp

src/main/webapp/WEB-INF/views/join.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<script type="text/javascript">
	function validateForm() {
		var id = document.forms["regForm"]["m_id"].value;
		var pw = document.forms["regForm"]["m_pw"].value;
		var name = document.forms["regForm"]["m_name"].value;
		var phone = document.forms["regForm"]["m_phone"].value;
		var img = document.forms["regForm"]["m_img"].value;
		
		if (id == "") {
			alert("아이디를 입력해주세요.")
			document.form["regForm"]{"m_id"}.focus();
			return false;
		}
		
		if (pw == "") {
			alert("비밀번호를 입력해주세요.")
			document.form["regForm"]{"m_pw"}.focus();
			return false;
		}
		
		if (name == "") {
			alert("이름을 입력해주세요.")
			document.form["regForm"]{"m_name"}.focus();
			return false;
		}
		
		var phoneRegex = /^[0-9]+$/;
		if (phone == "" || !phoneRegex.test(phone)) {
			alert("전화번호는 숫자만 입력 가능합니다.")
			document.form["regForm"]{"m_phone"}.focus();
			return false;
		}
		
		if (img == "") {
			alert("사진을 업로드해주세요.")
			return false;
		}
		return true;
	}
</script>
</head>
<body>
<form action="LoginController" method="post" onsubmit="return validateForm()" enctype="multipart/form-data">
	ID: <input type="text" name="m_id"> <p>   
	PW: <input type="password" name="m_pw"> <p>
	이름: <input type="text" name="m_name"> <p>
	생년월일: 
	<select name="m_birth_year">
		<% for(int i = 2026; i >= 1900; i--) {%>
			<option value="<%= i %>"><%= i %></option>
		<% } %>
	</select> 년
	<p>
	
	<select name="m_birth_month">
		<% for(int i = 1; i <= 12; i++) {%>
			<option value="<%= i %>"><%= i %></option>
		<% } %>
	</select> 월
	<p>
	
	<select name="m_birth_day">
		<% for(int i = 1; i <= 31; i++) {%>
			<option value="<%= i %>"><%= i %></option>
		<% } %>
	</select> 일
	<p>
	
	전화번호: <input type="text" name="m_phone"> <p>
	사진 업로드: <input type="file" name="m_img"> <p>
	<button type="submit">회원가입</button>
</form>

</body>
</html>
```

#### 이후 최종 수정본

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ page import="java.util.Calendar" %>

<%
	Calendar cal = Calendar.getInstance();
	int currentYear = cal.get(Calendar.YEAR);
%>

<section class="page-head">
	<p class="eyebrow">계정 생성 모드</p>
	<h2>회원가입</h2>
	<p>이 곳은 회원가입을 할 수 있는 곳 입니다.</p>
</section>

<div class="form-card">

	<form name="joinForm" action="#" method="post" enctype="multipart/form-data"
		  onsubmit="return joinCheck();">

		<div class="form-grid two">
			<div class="field">
				<label>아이디</label>
				<input type="text" name="m_id" placeholder="yourid">
			</div>

			<div class="field">
				<label>비밀번호</label>
				<input type="password" name="m_pw" placeholder="password">
			</div>
		</div>

		<div class="field">
			<label>이름</label>
			<input type="text" name="m_name" placeholder="yourname">
		</div>

		<div class="form-grid three">
			<div class="field">
				<label>태어난 연도</label>
				<select name="m_birth_year">
					<% for (int i = currentYear; i >= 1900; i--) { %>
						<option value="<%= i %>"><%= i %>년</option>
					<% } %>
				</select>
			</div>

			<div class="field">
				<label>월</label>
				<select name="m_birth_month">
					<% for (int i = 1; i <= 12; i++) { %>
						<option value="<%= i %>"><%= i %>월</option>
					<% } %>
				</select>
			</div>

			<div class="field">
				<label>일</label>
				<select name="m_birth_day">
					<% for (int i = 1; i <= 31; i++) { %>
						<option value="<%= i %>"><%= i %>일</option>
					<% } %>
				</select>
			</div>
		</div>

		<div class="field">
			<label>전화번호</label>
			<input type="text" name="m_phone" placeholder="01012345678">
		</div>

		<div class="field">
			<label>업로드할 사진</label>
			<input type="file" name="m_img">
		</div>

		<div class="form-actions">
			<button type="button" class="btn ghost" onclick="goPage('home');">취소</button>
			<button type="submit" class="btn primary">회원가입</button>
		</div>

	</form>
</div>
```

# index.css

src/main/webapp/css/index.css

```java
@charset "UTF-8";

* {
	box-sizing: border-box;
}

body {
	margin: 0;
	font-family: Arial, Helvetica, sans-serif;
	background:
		radial-gradient(circle at top left, #f0f4ff 0, transparent 34%),
		linear-gradient(135deg, #f7f7f8, #eceef2);
	color: #151515;
	min-height: 100vh;
}

a {
	color: inherit;
	text-decoration: none;
}

button,
input,
select,
textarea {
	font-family: inherit;
}

.app {
	width: min(1180px, calc(100% - 40px));
	margin: 0 auto;
}

.topbar {
	height: 88px;
	display: flex;
	align-items: center;
	justify-content: space-between;
}

.brand {
	display: flex;
	align-items: center;
	gap: 14px;
	cursor: pointer;
}

.brand-mark {
	width: 46px;
	height: 46px;
	border-radius: 14px;
	background: #111;
	color: #fff;
	display: grid;
	place-items: center;
	font-weight: 800;
	font-size: 22px;
	box-shadow: 0 14px 30px rgba(0, 0, 0, 0.18);
}

.brand h1 {
	margin: 0;
	font-size: 22px;
	letter-spacing: -0.5px;
}

.brand p {
	margin: 2px 0 0;
	color: #777;
	font-size: 13px;
}

.nav {
	display: flex;
	align-items: center;
	gap: 10px;
	background: rgba(255, 255, 255, 0.7);
	border: 1px solid rgba(255, 255, 255, 0.8);
	padding: 8px;
	border-radius: 999px;
	backdrop-filter: blur(12px);
	box-shadow: 0 12px 30px rgba(20, 20, 20, 0.08);
}

.nav a {
	padding: 10px 16px;
	border-radius: 999px;
	color: #555;
	font-size: 14px;
	font-weight: 700;
}

.nav a:hover {
	background: #111;
	color: #fff;
}

.main {
	min-height: calc(100vh - 170px);
	padding: 36px 0 70px;
}

.footer {
	padding: 30px 0;
	color: #888;
	font-size: 13px;
	text-align: center;
}

.hero {
	display: grid;
	grid-template-columns: 1.4fr 0.8fr;
	gap: 34px;
	align-items: stretch;
	margin-top: 30px;
}

.hero-copy,
.hero-card,
.feature-card,
.form-card,
.search-card,
.post-card,
.detail-card {
	background: rgba(255, 255, 255, 0.76);
	border: 1px solid rgba(255, 255, 255, 0.9);
	border-radius: 28px;
	box-shadow: 0 24px 70px rgba(30, 40, 60, 0.10);
	backdrop-filter: blur(14px);
}

.hero-copy {
	padding: 58px;
}

.eyebrow {
	margin: 0 0 14px;
	font-size: 12px;
	font-weight: 900;
	letter-spacing: 1.8px;
	color: #6c63ff;
	text-transform: uppercase;
}

.hero h2,
.page-head h2,
.board-head h2,
.detail-card h2 {
	margin: 0;
	font-size: clamp(34px, 5vw, 64px);
	letter-spacing: -2.3px;
	line-height: 1.02;
}

.hero-desc,
.page-head p,
.board-head p {
	margin: 22px 0 0;
	color: #666;
	font-size: 17px;
	line-height: 1.7;
}

.hero-actions {
	display: flex;
	gap: 12px;
	margin-top: 32px;
}

.btn {
	border: 0;
	border-radius: 14px;
	padding: 13px 18px;
	font-size: 14px;
	font-weight: 800;
	cursor: pointer;
	transition: 0.2s;
}

.btn:hover {
	transform: translateY(-2px);
}

.btn.primary {
	background: #111;
	color: #fff;
	box-shadow: 0 16px 30px rgba(0, 0, 0, 0.20);
}

.btn.ghost {
	background: #fff;
	color: #111;
	border: 1px solid #ddd;
}

.btn.dark {
	background: #27272a;
	color: #fff;
}

.btn.danger {
	background: #ff3b30;
	color: #fff;
}

.hero-card {
	padding: 34px;
	position: relative;
	overflow: hidden;
}

.status-dot {
	width: 13px;
	height: 13px;
	border-radius: 50%;
	background: #20c997;
	box-shadow: 0 0 0 8px rgba(32, 201, 151, 0.16);
	margin-bottom: 40px;
}

.hero-card h3 {
	font-size: 28px;
	margin: 0 0 18px;
	letter-spacing: -1px;
}

.hero-card ul {
	margin: 0;
	padding-left: 20px;
	color: #555;
	line-height: 2;
}

.feature-grid {
	display: grid;
	grid-template-columns: repeat(3, 1fr);
	gap: 18px;
	margin-top: 24px;
}

.feature-card {
	padding: 28px;
}

.feature-card span {
	font-size: 12px;
	font-weight: 900;
	color: #999;
}

.feature-card h3 {
	font-size: 24px;
	margin: 16px 0 10px;
}

.feature-card p {
	color: #666;
	line-height: 1.7;
	margin: 0;
}

.page-head,
.board-head {
	display: flex;
	justify-content: space-between;
	align-items: flex-end;
	gap: 24px;
	margin: 24px 0 24px;
}

.page-head {
	display: block;
	max-width: 760px;
}

.form-card {
	padding: 34px;
	max-width: 820px;
}

.form-card.small {
	max-width: 480px;
}

.form-grid {
	display: grid;
	gap: 16px;
}

.form-grid.two {
	grid-template-columns: repeat(2, 1fr);
}

.form-grid.three {
	grid-template-columns: repeat(3, 1fr);
}

.field {
	margin-bottom: 18px;
}

.field label {
	display: block;
	margin-bottom: 8px;
	font-size: 13px;
	font-weight: 900;
	color: #444;
}

.field input,
.field select,
.field textarea,
.search-card input {
	width: 100%;
	border: 1px solid #ddd;
	background: #fff;
	border-radius: 14px;
	padding: 14px 15px;
	font-size: 15px;
	outline: none;
	transition: 0.2s;
}

.field textarea {
	height: 220px;
	resize: vertical;
	line-height: 1.7;
}

.field input:focus,
.field select:focus,
.field textarea:focus,
.search-card input:focus {
	border-color: #111;
	box-shadow: 0 0 0 4px rgba(0, 0, 0, 0.08);
}

.form-actions {
	display: flex;
	justify-content: flex-end;
	gap: 10px;
	margin-top: 24px;
}

.danger-zone {
	margin-top: 24px;
	padding: 22px;
	background: #fff5f5;
	border: 1px solid #ffd4d4;
	border-radius: 20px;
}

.danger-zone h3 {
	margin: 0 0 6px;
	color: #c92a2a;
}

.danger-zone p {
	margin: 0 0 16px;
	color: #8a4b4b;
}

.board-head {
	margin-top: 22px;
}

.search-card {
	padding: 18px;
	margin-bottom: 18px;
}

.search-card form {
	display: flex;
	gap: 10px;
}

.post-list {
	display: grid;
	gap: 14px;
}

.post-card {
	padding: 26px;
	cursor: pointer;
	transition: 0.2s;
}

.post-card:hover {
	transform: translateY(-3px);
	box-shadow: 0 28px 80px rgba(20, 20, 20, 0.14);
}

.post-meta,
.detail-meta {
	display: flex;
	flex-wrap: wrap;
	gap: 10px;
	margin-bottom: 12px;
	color: #777;
	font-size: 13px;
	font-weight: 800;
}

.post-card h3 {
	margin: 0 0 10px;
	font-size: 23px;
	letter-spacing: -0.6px;
}

.post-card p {
	margin: 0;
	color: #666;
	line-height: 1.7;
}

.pagination {
	display: flex;
	justify-content: center;
	gap: 8px;
	margin-top: 24px;
}

.pagination a {
	width: 38px;
	height: 38px;
	display: grid;
	place-items: center;
	border-radius: 12px;
	background: #fff;
	border: 1px solid #ddd;
	font-weight: 800;
}

.pagination a:hover {
	background: #111;
	color: #fff;
}

.detail-card {
	padding: 42px;
	max-width: 900px;
}

.detail-card h2 {
	font-size: clamp(30px, 4vw, 48px);
}

.detail-content {
	margin-top: 26px;
	color: #555;
	line-height: 1.9;
	font-size: 17px;
}

.detail-actions {
	display: flex;
	gap: 10px;
	justify-content: flex-end;
	margin-top: 34px;
}

@media (max-width: 820px) {
	.topbar {
		height: auto;
		padding: 20px 0;
		align-items: flex-start;
		flex-direction: column;
		gap: 18px;
	}

	.nav {
		width: 100%;
		overflow-x: auto;
		justify-content: flex-start;
	}

	.hero {
		grid-template-columns: 1fr;
	}

	.hero-copy {
		padding: 34px;
	}

	.feature-grid,
	.form-grid.two,
	.form-grid.three {
		grid-template-columns: 1fr;
	}

	.board-head,
	.page-head {
		display: block;
	}

	.board-head .btn {
		margin-top: 18px;
	}

	.search-card form {
		flex-direction: column;
	}

	.form-actions,
	.detail-actions {
		flex-direction: column-reverse;
	}

	.btn {
		width: 100%;
	}
}
```
