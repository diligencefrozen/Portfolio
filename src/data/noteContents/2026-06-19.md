# 예제

## ex1 ~ ex9

```python
# -*- coding: utf-8 -*-
"""Jun19_1_Example.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1EzJ10gNGERaO5lDG3V0iRGSJPOzVb1Mh
"""

import numpy as np

# birth.txt 파일을 load하기
birth = np.loadtxt('births.txt',
                   delimiter = ",",
                   dtype=np.int64)

print(birth.shape)
print(type(birth))

birth

# 결과 : 연도, 남자아이 수, 여자아이 수

# 1. 남자아이, 여자아이 출생 수 평균을 구해주세요.
#       (정수로 출력)
x = np.mean(birth)
np.round(x).astype(int)
print(x)
#정답

print("남자아이 평균: ", int(birth[:,1].mean()))
print("여자아이 평균: ", int(birth[:,2].mean()))

# 2. 2000년대의 데이터를 구해주세요.
# 연도  남자  여자
# 2000  1234567  1234567
# ...
# 2024   ...     ...
y = birth[:, 0]
y_rs = birth[birth[:, 0] > 1999]
y_rs

#정답
print("연도", "남자", "여자", sep="\t")
print("=" * 30)
for y,m,f in birth[birth[:, 0] >= 2000]:
  print(y,m,f, sep="\t") # boolean indexing

# 3. 남자아이 출생이 가장 많은 연도와
#         남자아이 출생 수를 구해서 출력

m = birth[:, 1]
m_kid = np.max(m)

max_year = np.where(m==m_kid)[0]
m_year = birth[max_year, 0]
print(int(m_kid), "=> 남자아이 출생수")
print(m_year, "=> 출생이 가장 많은 연도")

#정답
#argmax() : 가장 많은 값의 index를 반환
birth[birth[:, 1].argmax()]
y = birth[birth[:, 1].argmax()][0]
b = birth[birth[:, 1].argmax()][1]
print("연도", "남자아이", sep="\t")
print(y,b,sep="\t")

# 4. 여자아이 출생이 가장 많은 top5를 구해주세요.
#     (연도, 여자아이 수 출력)
w = birth[:, 2]
w_kid = np.argsort(w)[::-1]

max_year = w_kid[:5]
#w_year = birth[max_year, 0]

#max_w = np.where(w==w_kid)[3]
#w_w = birth[max_w, 3]

#print(int(w_kid), "=>여자아이 수")
#print(int(w_year[0]), "=> 연도")
for i in max_year:
  y = int(birth[i,0])
  c = int(birth[i,2])
  print(y,"년")
  print(c,"명")

  #정답

  top5_girls = np.sort(birth[:,2])[-1:-6:-1]
  print(top5_girls)

  print("연도", "여자아이 수", sep="\t")
  print("=" * 20)
  for i in birth:
    for j in top5_girls:
      if i[2] == j:
        print(i[0], j, sep="\t") #sort 함수

# 5. 여자아이 출생률 (%) 을 구하고,
# 각 연도별로 여자아이 출생수가 차지하는 비율을
# 데이터로 보여주세요. (소수점 둘째자리까지)

# 연도    남자    여자    여아출생률
# 1880    123123    100000    41.xx
# ...
# 2024    ...       ...      ...
y = birth[:,0]
m = birth[:,1]
w = birth[:,2]

birthrate = m + w
womenbr = w / birthrate * 100
print("연도     남자      여자      여아출생률")
print("---------------------------------------")
for i in range(len(y)):
  print(f"{int(y[i])}     {int(m[i])}     {int(w[i])}     {int(womenbr[i])}")

w = birth[:,2]
w_mean = np.mean(w)
avg = np.round(w_mean).astype(int)
print(f"{w_mean:.2f} , <== 평균 여아출생률")

#정답

girls_ratio = ((birth[:,2]/(birth[:,1]+birth[:,2]))*100).round(2)
girls_ratio #round 함수

#zip 함수
print("연도", "남아", "여아", "여아 출생률", sep="\t")
print("=" * 40)
for y, b, g, gr in zip(birth[:,0], birth[:,1], birth[:,2], girls_ratio):
  print(y,b,g,gr,sep="\t")

# 6. 2000년대 데이터에 대해 출생률을 구하고,
#     남자아이 수가 1500000 이상이면 '많음'
#     아니면 '적음' 이라는 내용을 출력

# 연도   남자아이 수   결과
# 2000    1400000      적음
# ...
# 2024    ...          ...

y = birth[:, 0]
y_rs = birth[birth[:, 0] > 1999]

y = y_rs[:,0]
m = y_rs[:,1]
rs = y_rs[:,2]

print("연도   남자아이 수   결과")
print("-------------------------")
for i in range(len(y)):
  if m[i] >= 1500000:
    result ="많음"
  else:
    result = "적음"

  print(f"{int(y[i])}     {int(m[i])}     {result}")

#정답

after_2000 = birth[birth[:,0] >= 2000]
print(after_2000)

boys = after_2000[:,1]
print(boys)

amount = np.where(boys >= 1500000, "많음", "적음")
print(amount)

for y, b, amt in zip(after_2000[:,0], boys, amount):
  print(y,b,amt,sep="\t")

# 7. 1800년대, 1900년대, 2000년대의
#       각 남자아이, 여자아이 출생수 평균을 구해주세요.

# 1800년대
# 남아평균  여아평균
# 123123.xx  123123.xx

y = birth[:, 0]
y_rs = birth[(birth[:, 0] >= 1799) & (birth[:,0] <= 1899)]

y_rs1900 = birth[(birth[:, 0] >= 1899) & (birth[:,0] <= 1999)]

y_rs2000 = birth[(birth[:, 0] >= 1999) & (birth[:,0] <= 2099)]

y = y_rs[:,0]
m = y_rs[:,1]
rs = y_rs[:,2]

#1900
y2 = y_rs[:,0]
m2 = y_rs[:,1]
rs2 = y_rs[:,2]

#2000
y3 = y_rs[:,0]
m3 = y_rs[:,1]
rs3 = y_rs[:,2]

print("1800년대")
print("연도   남아평균   여아평균")
print("-------------------------")
for i in range(len(y)):

  print(f"{int(y[i])}     {int(m[i])}     {int(rs[i])}")

print("1900년대")
print("연도   남아평균   여아평균")
print("-------------------------")
for i in range(len(y2)):

  print(f"{int(y2[i])}     {int(m2[i])}     {int(rs2[i])}")

print("2000년대")
print("연도   남아평균   여아평균")
print("-------------------------")
for i in range(len(y3)):

  print(f"{int(y3[i])}     {int(m3[i])}     {int(rs3[i])}")

# 정답

print("1800년대")
print("남아 평균", "여아 평균")
k_1800s = birth[(birth[:,0] >= 1800) & (birth[:,0] < 1900)][:,1:]
#print(k_1800s)
print(k_1800s.mean(axis=0)[0],
      k_1800s.mean(axis=0)[1])

print("1900년대")
print("남아 평균", "여아 평균")
k_1900s = birth[(birth[:,0] >= 1900) & (birth[:,0] < 2000)][:,1:] # axis=0 (열 방향으로 계산)
#print(k_1800s)
print(k_1900s.mean(axis=0)[0],
      k_1900s.mean(axis=0)[1])

print("2000년대")
print("남아 평균", "여아 평균")
k_2000s = birth[(birth[:,0] >= 2000) & (birth[:,0] < 2100)][:,1:]
#print(k_1800s)
print(k_2000s.mean(axis=0)[0],
      k_2000s.mean(axis=0)[1])

# 8. 여자아이 출생수25% ~ 75% 사이의 값을 구해주세요.
#       (연도, 여자아이 출생 수)

#정답
# 사분위수 활용
g_q25 = np.quantile(birth[:,2], 0.25)
g_q75 = np.quantile(birth[:,2], 0.75)
print(g_q25, g_q75)
for y, g in birth[(birth[:,2] >= g_q25) &
                  (birth[:,2] <= g_q75)][:,[0,2]]:
                  print(y,g)

!sudo apt-get install -y fonts-nanum
!sudo fc-cache -fv
!rm ~/.cache/matplotlib -rf

# 9. 2000년대 남자아이 데이터를
#     bar 그래프로 표현해주세요.

#정답

import matplotlib.pyplot as plt
plt.rc("font", family="NanumBarunGothic")
plt.rcParams['axes.unicode_minus'] = False
b_2000s = birth[birth[:,0] >= 2000][:, 0:2]
#print(b_2000s)

plt.bar(b_2000s[:,0], b_2000s[:,1], color="salmon")
plt.title("2000년대 남자아이 출생량")
plt.xlabel("연도")
plt.ylabel("출생량")
plt.show()

```

https://colab.research.google.com/drive/1EzJ10gNGERaO5lDG3V0iRGSJPOzVb1Mh?usp=sharing

ㄴ 원본
