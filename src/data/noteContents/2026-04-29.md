# Input.html

```html
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>

<link rel="stylesheet" href="Input.css">
<script type="text/javascript" src="js/BeaverValidChecker.js"></script>
<script type="text/javascript" src="js/Checks.js"></script>
<script type="text/javascript">
	function checkValid() {
		let nameInput = document.bmiForm.name;
		let cmInput = document.bmiForm.cm;
		let kgInput = document.bmiForm.kg;
		let photoInput = document.bmiForm.photo;

		if (!nameInput.value) {
			alert("이름.");
			nameInput.focus();
			return false;
		}

		if (!cmInput.value) {
			alert("키.");
			cmInput.focus();
			return false;
		}

		if (isNaN(cmInput.value)) {
			alert("키는 숫자로.");
			cmInput.value = "";
			cmInput.focus();
			return false;
		}

		if (!kgInput.value) {
			alert("몸무게.");
			kgInput.focus();
			return false;
		}

		if (isNaN(kgInput.value)) {
			alert("몸무게는 숫자로.");
			kgInput.value = "";
			kgInput.focus();
			return false;
		}

		if (!photoInput.value) {
			alert("사진.");
			photoInput.focus();
			return false;
		}

		return true;
	}
</script>
</head>
<body>
	<!--
		BMI 검사
		BMI : 몸무게 / (키 * 키) => 키 m단위
		bmi가 18.5미만이면 저체중
				18.5이상이면 정상
				25이상이면 과체중
				30이상이면 경도비만
				35이상이면 중증도비만
				40이상이면 고도비만

		html -> 이름 키(cm) 몸무게(kg) 사진

		이름, 키, 몸무게, bmi 지수, 결과, 사진 을 servlet으로 출력
		+ 입력에 대한 유효성 검사 + 입력페이지 css

	 -->
	 <!--
	 <input id = "name">
	 <button ondblclick = "gobmi();">보내기</button>
	 -->

	 <!--
	 <form action="bmi" name="bmiform" enctype="multipart/form-data" onsubmit="return checkValid();"></form>
	 BMI <p id = "BMI"> <p>
	 이름 <input name = "name"> <p>
	 키 <input name = "cm"> <p>
	 몸무게 <input name = "kg"> <p>
	 사진 <input type = "file" name = "photo">
	 <hr>
	 <button>전송하기</button>
	 -->

<div id="product">

	 <form action="BMIOutput.jsp" name = bmiForm method="post" enctype="multipart/form-data" onsubmit="return bmiCheck();">
	 	<table id = "title">
	 		<tr>
	 			<td id = "menutitle" colspan="2">
	 				<a href="">BMI</a>
	 			</td>
	 		</tr>

		<tr>
			<td class="menu">이름</td>
			<td class="textTd">
				<input type="text" name="name" class="textBox" placeholder="이름" required>
			</td>
		</tr>

		<tr>
			<td class="menu">키</td>
			<td class="textTd">
				<input type="text" name="cm" class="textBox" placeholder="cm" required>
			</td>
		</tr>

		<tr>
			<td class="menu">몸무게</td>
			<td class="textTd">
				<input type="text" name="kg" class="textBox" placeholder="kg" required>
			</td>
		</tr>

		<tr>
			<td class="menu">사진</td>
			<td class="textTd">
				<input type="file" name="photo" class="textBox" placeholder="photo" required>
			</td>
		</tr>

		<tr>
			<td colspan="2" class="buttonTd">
				<button id="button">전송하기</button>
			</td>
		</tr>

	 	</table>

	 </form>
	 </div>

</body>
</html>
```

# Input.css

```css
@charset "UTF-8";

@font-face {
	/*
	웹폰트 등록 :
	CSS에 추가 (특정 브라우저에서 안 먹히는 경우도 !!)
	폰트파일에 있는 글자들 모양이 다 등록 => 용량이 커지고
								=> 느림...

	*/
	src: url("NotoSansKR-ExtraBold.ttf");
	font-family: "soso";
}

body {
	background-color: #fefefe;
	font-family: soso;

}

#menutitle a {
	display: inline-block;
	text-decoration: none;
	color: #fefefe;
	font-weight: 900;
	font-size: 2.8em;

}

#menutitle {
	text-align: center;
	padding-bottom: 25px;

}

#title {
	margin: 1px auto 0 auto;
	border-spacing: 0;
	background-color: #bbb2e9;
	border-radius: 0.82em;
	padding: 10px 30px;
	width: 700px;
}

#title td {
	padding: 12px 10px;
	vertical-align: top;
}

.menu {
	width: 250px;
	font-weight: bold;
	color: navy;
	text-align: right;
	padding-right: 20px;
}

.textTd {
	width: 350px;
	text-align: left;
}

.textBox {
	width: 220px;
	height: 30px;
	border-radius: 0.82em;
	text-align: center;
	box-sizing: border-box;
}

.textBox::placeholder {
	text-align: center;
}

#button {
	margin-top: 10px;
	margin-bottom: 10px;
	width: 6.5em;
	height: 4.5em;
	font-size: 16px;
	font-weight: bolder;
	cursor: pointer;
	border-radius: 0.82em;
	padding-left: 8px;

}

.buttonTd {
	text-align: center;
}

#product {
	width: 900px;
	margin: 40px auto;
	border: 20px solid #fefe00;   /* 노란색은 border에만 주기*/
	border-radius: 0.82em;
	padding: 40px;
	box-sizing: border-box;
	background-color: transparent; /* 안쪽 비우기 */
}

.textTd input placeholder {
	text-align: center;
}
```

# Check.js

```jsx
/**
 * 
 */

function bmiCheck() {
	let nameBox = document.bmiForm.name;
	let cmBox = document.bmiForm.cm;
	let kgBox = document.bmiForm.kg;
	let photoBox = document.bmiForm.photo;
	
	if (isEmpty(nameBox) || atLeastLetter(nameBox, 2)) {
		alert("Please Fill in your Name Properly.");
		nameBox.value = "";
		nameBox.focus();
		return false;
	} else if (isEmpty(cmBox) || isNotNumber(cmBox) ||atLeastLetter(cmBox, 4)) {
		alert("Please Fill in your Name Properly.");
		cmBox.value = "";
		cmBox.focus();
		return false;
	} else if (isEmpty(kgBox) || isNotNumber(kgBox) ||atLeastLetter(kgBox, 4)) {
		alert("Please Fill in your Name Properly.");
		kgBox.value = "";
		kgBox.focus();
		return false;
	} else if (isEmpty(photoBox) || (isNotType(photoBox, "png") && (isNotType(photoBox, "gif") && (isNotType(photoBox, "jpg") &&(isNotType(photoBox, "bmp")))))) {
		alert("Please Fill in your Name Properly.");
		photoBox.value = "";
		return false;
	}
	return true;
	
}
```

# BeaverValidChecker.js

```jsx
/**
 * 
 */
// 잘못됐으면 true값
//(뭔가 문제가 되면 true, 정상적이면 false)

// <input> 을 넣었을 때
// 아무것도 안썼으면 true, 뭐라도 썼으면 false
function isEmpty(input) {
	return (!input.value);

}

// <input>을 넣었을떄
// 한글/ 특수문자 적혀있다면 true, 없다면 false
function containsAnother(input) {
	let pass = "1234567890zxcvbnmasdfghjklqwertyuiopZXCVBNMASDFGHJKLQWERTYUIOP@_.";
	let iv = input.value;
	for (let i = 0; i < iv.length; i++) {
		if (pass.indexOf(iv[i]) == -1) { // pass 에 있는 내용중에서 내가 쓴 내용이 없으면
			
			return true;
			}
	}
	return false;
}

// <input>, 글자 수를 넣었을 때
// 그 글자 수 보다 적으면 true, 그 글자수 이상이면 false
function atLeastLetter(input, len) {
	return (input.value.length < len);
}

// <input> x 2 넣었을 때
// 내용이 다르면 true, 같으면 false (pw, pw 확인)
function notEqualPw(input1, input2) {
	return (input1.value != input2.value);
}

// <input>, 문자열세트 넣었을 때
// 없으면 true, 있으면 false, (입력한 pw에 지정한 문자열이 없는 경우)
function notContain(input, passSet) {
	let iv = input.value;
	for (let i = 0; i < passSet.length; i++) {
		if (iv.indexOf(passSet[i]) != -1) {
			return false;
		}
	}
	return true;
}

// <input> 넣었을 때 
//숫자 아닌거 있으면 true, 숫자만 있으면 false
function isNotNumber(input) {
	return isNaN(input.value); 
}

// <input>, 확장자 넣었을 때
// 확장자명이 아니면 true, 맞으면 false
function isNotType(input, type) {
	// .png, .gif, .bmp, ...
	type = "." + type;
	return (input.value.inderOf(type) == -1);
}
```

# 두가지 Output 방식

## BMIOutput.java

```java
package com.beaver.apr283.main;
import java.io.IOException;
import java.io.PrintWriter;
import java.net.URLEncoder;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.oreilly.servlet.MultipartRequest;
import com.oreilly.servlet.multipart.DefaultFileRenamePolicy;

@WebServlet("/BMIOutput")
public class BMIOutput extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    public BMIOutput() {
        super();
        
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		response.setCharacterEncoding("UTF-8");
		String path = request.getServletContext().getRealPath("img");
		System.out.println(path);
		
		MultipartRequest mr = new MultipartRequest(request, path, 30 * 1024 * 1024, "UTF-8", new DefaultFileRenamePolicy());
		
		String name = mr.getParameter("name");
		double cm = Double.parseDouble(mr.getParameter("cm"));
		double kg = Double.parseDouble(mr.getParameter("kg"));
		cm /= 100;
		double bmi = kg / (cm*kg);
		
		String result = "저체중";
		if (bmi >= 40) {
			result = "고도비만";
		} else if (bmi >= 35) {
			result = "중증도비만";
		} else if (bmi >= 30) {
			result = "경도비만";
		} else if (bmi >= 25) {
            result = "과체중";
		} else if (bmi >= 18.5) {
			result = "정상";
		}
		
		String image = mr.getFilesystemName("photo");
		image = URLEncoder.encode(image, "UTF-8");
		image = image.replace("+"," ");
		
		PrintWriter out = response.getWriter();
		out.print("<html>");
		out.print("<head><title>BMI Result</title>");
		out.print("<meta charset='UTF-8'></head>");
		out.print("<body>");
		
		out.print("<h1>BMI 결과</h1>");
		out.printf("이름: %s<p>", name);
		out.printf("키: %fcm<p>", cm);
		out.printf("몸무게: %.1fkg<p>", kg);
		out.printf("BMI: %.2f<p>", bmi);
		out.printf("<h2>당신은 %s 입니다.</h2>", result);
		
		out.printf("<img src='img/%s' style='max-width: 400px;'>", image);
		
		out.print("</body>");
		out.print("</html>");
	}

}

```

## BMIOutput.jsp

```java
<%@page import="java.net.URLEncoder"%>
<%@page import="com.oreilly.servlet.multipart.DefaultFileRenamePolicy"%>
<%@page import="com.oreilly.servlet.MultipartRequest"%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>

</head>
<body>

	<%
	response.setCharacterEncoding("UTF-8");
	String path = request.getServletContext().getRealPath("img");
	System.out.println(path);
	
	MultipartRequest mr = new MultipartRequest(request, path, 30 * 1024 * 1024, "UTF-8", new DefaultFileRenamePolicy());
	
	String name = mr.getParameter("name");
	double cm = Double.parseDouble(mr.getParameter("cm"));
	double kg = Double.parseDouble(mr.getParameter("kg"));
	cm /= 100;
	double bmi = kg / (cm*kg);
	
	String result = "저체중";
	if (bmi >= 40) {
		result = "고도비만";
	} else if (bmi >= 35) {
		result = "중증도비만";
	} else if (bmi >= 30) {
		result = "경도비만";
	} else if (bmi >= 25) {
        result = "과체중";
	} else if (bmi >= 18.5) {
		result = "정상";
	}
	
	// BMI 수치 => 소수점 둘째 까지만
	// (HTML에서 나타낼수 있는 방법을 몰라서 여기서 해결!)
	String bmi2 = String.format("%.2f", bmi);
	
	String image = mr.getFilesystemName("photo");
	image = URLEncoder.encode(image, "UTF-8");
	image = image.replace("+"," ");
	%>
	
	<table>
		<tr>
			<th colspan="2">BMI Result</th>
		</tr>
		<tr>
			<td align="center">이름</td>
			<td><%=name %></td>
		</tr>	
		<tr>
			<td align="center">키</td>
			<td><%=cm * 100 %>cm</td>
		</tr>	
		<tr>
			<td align="center">몸무게</td>
			<td><%=kg %>kg</td>
		</tr>	
		<tr>
			<td align="center">BMI</td>
			<td><%=bmi2 %></td>
		</tr>
		<tr>
			<td align="center" colspan="2">
				<marquee behavior = "alternate">
				당신은 <%=result %>입니다.
				</marquee>
			</td>
		</tr>
		<tr>
			<td align="center" colspan="2">
				<img src="img/<%=image %>" style="max-width: 40%;">

				
			</td>
		</tr>
		
	</table>

</body>
</html>
```

---

### HTML : 웹사이트 제작 가능

→ 

단점으로는 디자인과 이벤트 부족 문제 : 못생기고, 정적인 이벤트만 가능.

HTML : 뼈대를 만들고,

CSS : 디자인

JavaScript : 이벤트 처리

→ 파라미터 읽기, 계산, 조건문, 반복문 : 프로그래밍 언어쪽의 기능이 부재.

⇒ 이걸 극복하기 위한 해결책 : Servlet

### Servlet : 클라이언트가 요청하면 HTML + CSS + JavaScript를 만들어서 응답

⇒ 어렵고, 작업이 불편함.

# JSP(Java Servlet/Server Page)

: 작업 형태 = HTML + CSS + JavaScript에 필요로 하는 부분마다 Java 코드를 살짝 첨가 !

실제 정체 : Tomcat이 Servlet 으로 바꿔서 실행 !

Servlet : 자바 코드 안에서 HTML을 구현 

JSP : HTML 안에서 자바 코드를 추가

```java
<%--

--%>
```

# 1.스크립트릿 (Scriptlet)

JSP에서 Java 코드를 실행할 때 사용하는 블록

```java
<% Java 코드가 여기에!! %>
```

# 2. 지시자

특별한 지시를 내릴 수 있도록 하는 블록

```java
<%@ page / include / taglib / ... %>
```

# 3. 표현식

어떤 값을 (웹페이지에) 출력 결과로 포함시키고 싶을때 사용하는 블록

```java
<%= 값 / 변수 / ... %>
```

숫자, 문자열, 변수 등 값 사용 가능

Servlet의 out.print() 와 같은 기능

# 4. 선언문

표현식에서 사용할 수 있는 Java의 method를 작성할 때 사용하는 블록

```java
<@! 멤버변수 / method %>
```

Java의 method와 동일 (심지어는 이름 짓는 규칙까지도)

# 5. 주석

```java
<%--  주석들주석들 --%>
```

# j2_basic.jsp

내 코드 

```java

Scanner x = new Scanner(System.in);
System.out.println("입력: ");
int num = x.nextInt();
if (num % 2 == 0) {
	System.out.println("짝");
} else {
	System.out.println("홀");
}

```

정답 코드

```java
<%@page import="java.util.Random"%>
<%@page import="java.util.Scanner"%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

	<%-- 
		랜덤한 정수를 넣었을 때, 짝인지 홀인지 알려주는 기능
		뽑은 숫자와 홀짝 여부를 웹페이지에 출력	
	--%>
	
	<%!
		public String getOE(int x) {
			return (x%2==0) ? "짝수" : "홀수";
		}
	%>
	
	<%
		Random r = new Random();
		int x = r.nextInt();
	%>
	
	<h1>뽑은 숫자 : <%=x %></h1>
	<h1>뽑은 숫자 :<%=getOE(x) %></h1>
	

</body>
</html>
```

# j3_gugudan.jsp

내 코드

```java
<%!

**public** **static** **void** main(String[] args) {

**for** (**int** i = 2; i <= 9; i++) {

**for** (**int** dan = 2; dan <= 9; dan++) {

System.out.printf("%d x %d = %d\t", dan, i, dan * i);

}

System.out.println();

}

}

%>

<h1></h1>
```

정답 코드

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

	<% for (int i = 2; i <= 9; i++) { %>
	<table border = "1">
	<tr>
		<th><%=i %> 단</th>	
	</tr>
	<% for (int j = 1; j <= 9; j++) { %>
	<tr>
		<td><%=i %> x <%=j %> = <%=i*j %></td>
    </tr>
    <% 
    		} 
    		
    %>
    </table>
    <% 
    	} 
    %>

	

</body>
</html>
```

# j3_gugudan.jsp - 가로로 나열

내 코드

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

	<% for (int i = 2; i <= 9; i++) { %>
	<table border = "6" style="float:left" style="border:5px solid transparent">
	<tr>
		<th><%=i %> 단</th>	
	</tr>
	<% for (int j = 1; j <= 9; j++) { %>
	<tr>
		<td><%=i %> x <%=j %> = <%=i*j %></td>
    </tr>
    <% 
    		} 
    		
    %>
    </table>
    <% 
    	} 
    %>

	

</body>
</html>
```

정답 코드

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<style type="text/css">
	table {
		float: left;
		margin-right: 10px;
	}

</style>
</head>
<body>

	<% for (int i = 2; i <= 9; i++) { %>
	<table border = "1">
	<tr>
		<th><%=i %> 단</th>	
	</tr>
	<% for (int j = 1; j <= 9; j++) { %>
	<tr>
		<td><%=i %> x <%=j %> = <%=i*j %></td>
    </tr>
    <% 
    		} 
    		
    %>
    </table>
    <% 
    	} 
    %>

	

</body>
</html>
```

# j4_input.html

내 코드

```java
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<!-- 
		이름, x, y(정수) 입력해서 버튼 누르면
		j4_Output.jsp로 GET 방식 요청해서 
		이름, x와 y 더한 값 출력
	 -->
	 
	 <form action="j4_Output.jsp" method="get"> <!-- get 은 굳이 명시안해도됨 -->
	 	<tr>
	 		<td>
	 			이름
	 		</td>
	 		<td>
	 			<input type="text" name="name" required"> <p>
	 		</td>
	 	</tr>
	 	
	 	<tr>
	 		<td>
	 			x
	 		</td>
	 		<td>
	 			<input type="text" name="x" required"> <p>
	 		</td>
	 	</tr>
	 	
	 	<tr>
	 		<td>
	 			y
	 		</td>
	 		<td>
	 			<input type="text" name="y" required"> <p>
	 		</td>
	 	</tr>
	 	
	 	<tr>
	 		<td colspan="2">
	 			<button>전송</button>
	 		</td>
	 	</tr>
	 </table>
	 
	 </form>
	 
	 
</body>
</html>
```

# j4_Output.jsp

정답 코드

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

	<%
		// GET / POST
		// 원래) 요청파라미터 => 한글 처리
		//		GET - 안해도됨
		//		POST - 해야함.
		request.setCharacterEncoding("UTF-8");
		
		// 원래) 응답 한글처리를 해야했는데
		// 위에서 해주고 있어서 필요없음!
		// ex ) 1~2번줄, 6번줄
		//response.setCharacterEncoding("UTF-8");
		
		String name = request.getParameter("name");
		int x = Integer.parseInt(request.getParameter("x"));
		int y = Integer.parseInt(request.getParameter("y"));
		int z = x + y;
		
		
	
	
	
	
	
	
	
	
	
	
	
	
	%>
	이름 : <%=name %> <p>
	<h1><%=x %> + <%=y %>= <%=z %></h1>

</body>
</html>
```

GET / POST
원래) 요청파라미터 => 한글 처리
		GET - 안해도됨
		POST - 해야함.

원래) 응답 한글처리를 해야했는데
위에서 해주고 있어서 필요없음!
ex ) 1~2번줄, 6번줄
response.setCharacterEncoding("UTF-8");

# input.html

```java
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<!-- 
		<input>에 ex) 1,2,3,o,z,4 입력했을 때
		output.jsp 에서 숫자인것들만 다 더한 값을 출력
		(split, StringTokenizer 둘 다 사용)
	-->
	
	<form action="input.jsp">
	 	<tr>
	 		<td>
	 			아무거나 입력하시오:
	 		</td>
	 		<td>
	 			<input type="text" name="words" required"> <p>
	 		</td>
	 	</tr>
	 	
	 	<tr>
	 		<td colspan="2">
	 			<button>전송</button>
	 		</td>
	 	</tr>
	
	
	</form>
	
	

</body>
</html>
```

# output.jsp

```java
<%@page import="java.util.StringTokenizer"%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

	<%
	response.sendRedirect("test.jsp");
	request.setCharacterEncoding("UTF-8");
	

	String words_num = request.getParameter("words");
	
	// Split
	String[] onlyNum = words_num.split(",");
	
	int sum1 = 0;
	for (String n : onlyNum) {
		try {
			sum1 += Integer.parseInt(n);
		} catch (Exception e) {
			
		}
		
	}
	
	// StringTokenizer
	StringTokenizer st = new StringTokenizer(words_num, ",");
	int sum2 = 0;
	while (st.hasMoreTokens()) {
		try {
			sum2 += Integer.parseInt(st.nextToken());
		} catch (Exception e) {
			
		}
	}
	
	
	
	%>
	
	합(Split) : <%=sum1 %> <br>
	합(StringTokenizer) : <%=sum2 %>

</body>
</html>
```

# test.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

	<!-- 
		입력페이지(input.html) => 출력페이지(output.jsp)
							 ㄴ 점검중 (test.jsp) 로 보내자
							 
		페이지 이동
		
			수동(사용자가 액션을 하면 아동) - 요청
				-<a> : 클릭하면 href 속성의 주소로 이동
				- <form> + <button>
				-JS (location.href) : 이벤트를 지정할수 있음
			
			자동(사용자의 액션과는 상관없이)
				-redirect
				output.jsp의 <body> 부분 제일 처음에 (자바코드 안에서)
				response.sendRedirect("test.jsp");
	 -->
	 점검중... (2026-04-29 수요일 PM 4:43~ ???)

</body>
</html>
```
