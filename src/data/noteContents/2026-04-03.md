```jsx
package com.jeesung.exam01.today.test;

import java.lang.reflect.Array;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.Scanner;

public class AttendanceSystem {
	
	// 1. 메뉴 출력
	public static void printMenu() {
		System.out.println("===== 출석 관리 시스템 =====");
		System.out.println("1. 학생 등록");
		System.out.println("2. 출석 체크");
		System.out.println("3. 출석 현황 출력");
		System.out.println("4. 출석률 분석");
		System.out.println("5. 종료");
		System.out.println("========================");
	}
	
	// 1-2 메뉴 고르기
	public static int getMenuNum() {
		Scanner k = new Scanner(System.in);
		System.out.println("선택>");
		int selectNo = k.nextInt();
		if ((selectNo >= 1 && selectNo <= 5)) {
			System.out.println("해당 메뉴는 존재하지 않습니다.");
			printMenu();
			
		}
		return (selectNo >= 1 && selectNo <= 5) ? selectNo : getMenuNum();
	}
	
	// 2. 학생등록 + 학생 중복처리
	public static void registerStudent(ArrayList<Student> students) {
		System.out.println("등록할 학생 이름:");
		Scanner k = new Scanner(System.in);
		String name = k.next();
		if (isDuplicate(name, students)) {
			System.out.println("이미 등록된 학생입니다.");
		} else { 
			students.add(new Student(name));
			System.out.println("학생 등록 완료!");
		}
	}
	
	// 2-1. 이름이 중복되었을 때 (중복되었으면 true)
	public static boolean isDuplicate(String name, ArrayList<Student> students) {
			for (Student s : students) {
				if (s.getName().equals(name)) {
					return true;
				}
			}
			return false;
		}
	
	
	// 3. 출석 기능
	public static void checkAttendance(ArrayList<Student> students) {
		Scanner k = new Scanner(System.in);
		System.out.println("출석할학생이름:");
		String name = k.next();
		for (Student s : students) {
			if (s.getName().equals(name)) {
				if (s.isCheckedIn()) {
					System.out.println("이미 출석 처리되었습니다.");
				} else {
					s.checkIn();
					System.out.println("출결 완료 !");
				}
				return;
			}
		}
		System.out.println("해당 학생이 존재하지 않습니다.");
	}
	
	// 4. 출석현황 출력
	public static void printAttendance(ArrayList<Student> students) {
		System.out.println("====== 출석 현황 ======");
		if (students.isEmpty()) { // students == null
			System.out.println("등록된 학생이 없습니다.");
			return;
		}
		for (Student s : students) {
			s.printInfo();
		}
	}.0
	
	// 5. 출석률 출력
	public static void analayzeAttendance(ArrayList<Student> students) {
		if (students.isEmpty()) {
			System.out.println("등록된 학생이 없습니다.");
			return;
		}
		int count = 0;
		for (Student s : students) {
			if (s.isCheckedIn()) {
				count++;
			}
		}
		double rate = (double) count / students.size();
		System.out.printf("출석률 : %.2f%% (%d / %d명\n", rate,count,students.size());
	}
	
	// 6. 종료
	public static void end() {
		System.out.println("프로그램을 종료합니다.");
	}
	
	public static void start() {
		int selectNo = 0;
		while (true) {
			printMenu();
			selectNo = getMenuNum();
			ArrayList<Student> students = new ArrayList<Student>();
			while (true) {
				printMenu();
				selectNo = getMenuNum();
				if (selectNo == 1) {
					registerStudent(students);
				} else if (selectNo == 2) {
					checkAttendance(students);
				} else if (selectNo == 3 ) {
					printAttendance(students);
				} else if (selectNo == 4) {
					analayzeAttendance(students);
				} else if (selectNo == 5) {
					end();
					break;
				}
			}
		}
	}

}

```

# AttendanceSystem

---

```jsx
package com.jeesung.exam01.today.test;

public class Student {
	private String name;
	private boolean checkedIn;
	
	public Student(String name) {
		super();
		this.name = name;
		this.checkedIn = false;
	}
	
	public void checkIn() {
		this.checkedIn = true;
	}

	public String getName() {
		return name;
	}

	public void setName(String name) {
		this.name = name;
	}

	public boolean isCheckedIn() {
		return checkedIn;
	}

	public void setCheckedIn(boolean checkedIn) {
		this.checkedIn = checkedIn;
	}
	
	public void printInfo() {
		System.out.println(name + " - 출석: "+ (checkedIn ? "O" : "X"));
		
	}
	
	
	
	

}

```

## Student

```jsx
package com.jeesung.exam01.today.test;

public class Main {
	public static void main(String[] args) {
		AttendanceSystem.start();
	}

}

```

## Main

---

## 1. 전체 프로그램 구조

이 프로그램은 **학생 출석 관리 시스템.**

### 클래스 역할

- `t1` : 기능 담당
- `t2` : 학생 1명의 정보 담당
- `t3` : 실행 담당

### 한 줄 요약

- `t1` = 선생님
- `t2` = 학생
- `t3` = 시작 버튼

---

# 2. t2 클래스 정리

## 역할

학생 한 명의 정보를 저장하는 클래스

## 필드

```java
private String name;
private boolean checkedIn;
```

### 의미

- `name` : 학생 이름
- `checkedIn` : 출석 여부
    - `true` = 출석
    - `false` = 결석

---

## 생성자

```java
public t2(String name) {
    this.name = name;
    this.checkedIn = false;
}
```

### 의미

- 학생 객체를 만들 때 이름을 저장
- 처음에는 출석하지 않았으므로 `false`

---

## 메서드

### 출석 처리

```java
public void checkIn() {
    this.checkedIn = true;
}
```

### 이름 반환

```java
public String getName() {
    return name;
}
```

### 출석 여부 반환

```java
public boolean isCheckedIn() {
    return checkedIn;
}
```

### 학생 정보 출력

```java
public void printInfo() {
    System.out.println(name + " - 출석: " + (checkedIn ? "O" : "X"));
}
```

---

# 3. t1 클래스 정리

## 역할

출석 관리 기능 전체 담당

### 주요 기능

1. 메뉴 출력
2. 학생 등록
3. 출석 체크
4. 출석 현황 출력
5. 출석률 분석
6. 종료

---

# 4. 메뉴 출력

## 메서드

```java
public static void printMenu()
```

## 역할

사용자에게 메뉴 보여주기

## 출력 내용

```
===== 출석 관리 시스템 =====
1. 학생 등록
2. 출석 체크
3. 출석 현황 출력
4. 출석률 분석
5. 종료
========================
```

---

# 5. 메뉴 번호 입력

## 메서드

```java
public static int getMenuNum()
```

## 역할

사용자로부터 메뉴 번호 입력받기

## 핵심

- `Scanner` 사용
- 번호가 1~5인지 검사
- 잘못 입력하면 다시 입력받음

---

## 주의할 점

현재 코드:

```java
if ((selectNo >= 1 && selectNo <= 5)) {
    System.out.println("해당 메뉴는 존재하지 않습니다.");
    printMenu();
}
```

이건 **조건이 반대.**

### 수정해야 할 형태

```java
if (selectNo < 1 || selectNo > 5) {
    System.out.println("해당 메뉴는 존재하지 않습니다.");
    printMenu();
}
```

---

# 6. ArrayList 정리

## 선언

```java
ArrayList<t2> students = new ArrayList<t2>();
```

## 의미

학생 여러 명을 저장하는 리스트

### 쉽게 말하면

- `t2` = 학생 1명
- `ArrayList<t2>` = 학생 명단

---

## 왜 사용하는가

학생 수가 몇 명일지 모르기 때문

배열은 크기가 고정이고, `ArrayList`는 크기가 자동으로 늘어남

---

## 자주 쓰는 기능

### 추가

```java
students.add(new t2(name));
```

### 비어있는지 확인

```java
students.isEmpty();
```

### 개수 확인

```java
students.size();
```

### 반복

```java
for (t2 s : students)
```

---

# 7. 학생 등록 기능

## 메서드

```java
public static void registerStudent(ArrayList<t2> students)
```

## 역할

학생 이름을 입력받아 리스트에 추가

---

## 동작 순서

1. 이름 입력받기
2. 중복 검사
3. 중복 아니면 학생 추가

---

## 핵심 코드

```java
if (isDuplicate(name, students)) {
    System.out.println("이미 등록된 학생입니다.");
} else {
    students.add(new t2(name));
    System.out.println("학생 등록 완료!");
}
```

---

# 8. 중복 검사 기능

## 메서드

```java
public static boolean isDuplicate(String name, ArrayList<t2> students)
```

## 역할

같은 이름의 학생이 이미 존재하는지 확인

---

## 핵심 코드

```java
for (t2 s : students) {
    if (s.getName().equals(name)) {
        return true;
    }
}
return false;
```

---

- 리스트 탐색
- 문자열 비교는 `==` 말고 `equals()` 사용

---

# 9. 출석 체크 기능

## 메서드

```java
public static void checkAttendance(ArrayList<t2> students)
```

## 역할

이름을 입력받아 해당 학생 출석 처리

---

## 동작 순서

1. 이름 입력
2. 리스트에서 학생 찾기
3. 이미 출석했는지 확인
4. 출석 처리 또는 안내 출력

---

## 핵심 코드 구조

```java
for (t2 s : students) {
    if (s.getName().equals(name)) {
        if (s.isCheckedIn()) {
            System.out.println("이미 출석 처리되었습니다.");
        } else {
            s.checkIn();
            System.out.println("출결 완료 !");
        }
        return;
    }
}
System.out.println("해당 학생이 존재하지 않습니다.");
```

---

## 포인트

- 검색: `for`
- 비교: `equals()`
- 상태 확인: `isCheckedIn()`
- 상태 변경: `checkIn()`
- 찾으면 `return`으로 종료

---

# 10. 출석 현황 출력 기능

## 메서드

```java
public static void printAttendance(ArrayList<t2> students)
```

## 역할

전체 학생의 출석 상태 출력

---

## 동작 순서

1. 제목 출력
2. 리스트가 비어있는지 검사
3. 전체 학생 출력

---

## 핵심 코드

```java
System.out.println("====== 출석 현황 ======");
if (students.isEmpty()) {
    System.out.println("등록된 학생이 없습니다.");
    return;
}
for (t2 s : students) {
    s.printInfo();
}
```

---

# 11. 출석률 분석 기능

## 메서드

```java
public static void analayzeAttendance(ArrayList<t2> students)
```

## 역할

출석한 학생 수를 세고 출석률 계산

---

## 동작 순서

1. 학생이 있는지 확인
2. 출석한 학생 수 세기
3. 출석률 계산
4. 출력

---

## 핵심 코드

```java
int count = 0;
for (t2 s : students) {
    if (s.isCheckedIn()) {
        count++;
    }
}
double rate = (double) count / students.size();
```

---

## 주의할 점

현재 코드:

```java
System.out.printf("출석률 : %.2f%% (%d / %d명\n", rate,count,students.size());
```

문제점 2개:

1. 퍼센트로 출력하려면 `100` 해야 함
2. 괄호 `)` 빠짐

### 수정 코드

```java
double rate = (double) count / students.size() * 100;
System.out.printf("출석률 : %.2f%% (%d / %d명)\n", rate, count, students.size());
```

---

# 12. 종료 기능

## 메서드

```java
public static void end()
```

## 역할

프로그램 종료 메시지 출력

```java
System.out.println("프로그램을 종료합니다.");
```

---

# 13. start() 메서드 정리

## 역할

프로그램 전체 실행

---

## 현재 구조 핵심

메뉴를 계속 반복해서 보여주고

선택한 번호에 따라 기능 실행

---

## 핵심 개념

- `while(true)` : 계속 반복
- 메뉴 선택
- `if ~ else if` 로 기능 분기

---

## 현재 코드 문제점

```java
while (true) {
    printMenu();
    selectNo = getMenuNum();
    ArrayList<t2> students = new ArrayList<t2>();
    while (true) {
        ...
    }
}
```

### 문제

`students`가 반복문 안에서 계속 새로 만들어짐

즉, 학생 정보가 유지되지 않을 수 있음

---

## 바른 구조

```java
public static void start() {
    int selectNo = 0;
    ArrayList<t2> students = new ArrayList<t2>();

    while (true) {
        printMenu();
        selectNo = getMenuNum();

        if (selectNo == 1) {
            registerStudent(students);
        } else if (selectNo == 2) {
            checkAttendance(students);
        } else if (selectNo == 3) {
            printAttendance(students);
        } else if (selectNo == 4) {
            analayzeAttendance(students);
        } else if (selectNo == 5) {
            end();
            break;
        }
    }
}
```

---

# 14. t3 클래스 정리

## 역할

프로그램 시작용 메인 클래스

## 코드

```java
public class t3 {
    public static void main(String[] args) {
        t1.start();
    }
}
```

## 의미

`main()`이 실행되면 `t1.start()`를 호출해서 전체 프로그램 시작

---

## 핵심 개념 정리

- 클래스
- 객체
- 생성자
- 메서드
- getter / setter
- boolean
- 조건문 `if`
- 반복문 `for`, `while`
- `ArrayList`
- `Scanner`
- 문자열 비교 `equals()`

---

# 16. 패턴

## 1) 리스트 선언

```java
ArrayList<t2> students = new ArrayList<t2>();
```

## 2) 객체 추가

```java
students.add(new t2(name));
```

## 3) 전체 탐색

```java
for (t2 s : students)
```

## 4) 이름 비교

```java
s.getName().equals(name)
```

## 5) 비어있는지 확인

```java
students.isEmpty()
```

## 6) 개수 확인

```java
students.size()
```

---

### 도서시스템

```jsx
package com.jeesung.exam01.today.test;

import java.util.ArrayList;
import java.util.Scanner;

public class AttendanceSystem {
	
	public static void printMenu() {
		System.out.println("===== 도서 대출 시스템 =====");
		System.out.println("1. 도서 등록");
		System.out.println("2. 도서 대출");
		System.out.println("3. 도서 현황 출력");
		System.out.println("4. 대출률 분석");
		System.out.println("5. 종료");
		System.out.println("=======================");
	}
	
	public static int getMenuNum() {
		Scanner k = new Scanner(System.in);
		System.out.println("선택>");
		int selectNo = k.nextInt();
		if ((selectNo >= 1 && selectNo <= 5)) {
			System.out.println("해당 메뉴는 존재하지 않습니다.");
			printMenu();
		}
		return (selectNo >= 1 && selectNo <= 5) ? selectNo : getMenuNum();
	}
	
	public static void registerStudent(ArrayList<Student> students) {
		System.out.println("등록할 도서명:");
		Scanner k = new Scanner(System.in);
		String name = k.next();
		if (isDuplicate(name, students)) {
			System.out.println("이미 등록된 도서입니다.");
		} else {
			students.add(new Student(name));
			System.out.println("도서 등록 완료!");
		}
	} 
	
	public static boolean isDuplicate(String name, ArrayList<Student> students) {
		for (Student s : students) {
			if (s.getName().equals(name)) {
				return true;
			}
		}
		return false;
	}

	public static void checkAttendance(ArrayList<Student> students) {
		Scanner k = new Scanner(System.in);
		System.out.println("대출할 도서명:");
		String name = k.next();
		for (Student s : students) {
			if (s.isCheckedIn()) {
				System.out.println("이미 대출된 도서입니다.");
			} else {
				s.checkIn();
				System.out.println("대출 완료 !");
			}
			return;
		}
		System.out.println("해당 도서가 존재하지 않습니다.");
	}
	
	public static void printAttendance(ArrayList<Student> students) {
		System.out.println("=== 도서 현황 ===");
		if (students.isEmpty()) {
			System.out.println("등록된 도서가 없습니다.");
			return;
		}
		for (Student s : students) {
			s.printInfo();
		}
	}
	
	public static void analayzeAttendance(ArrayList<Student> students) {
		if (students.isEmpty()) {
			System.out.println("등록된 도서가 없습니다.");
			return;
			
		}
		int count = 0;
		for (Student s : students) {
			if (s.isCheckedIn()) {
				count++;
			}
		}
		double rate = (double) count / students.size();
		System.out.printf("대출률 : %.2f%% (%d / %d권)\n", rate, count, students.size());
	}
	
	public static void end() {
		System.out.println("프로그램을 종료합니다.");
	}
	
	public static void start() {
		int selectNo = 0;
		while (true) {
			printMenu();
			selectNo = getMenuNum();
			ArrayList<Student> students = new ArrayList<Student>();
			while (true) {
				printMenu();
				selectNo = getMenuNum();
				if (selectNo == 1) {
					registerStudent(students);
				} else if (selectNo == 2) {
					checkAttendance(students);
				} else if (selectNo == 3) {
					printAttendance(students);
				} else if (selectNo == 4) {
					analayzeAttendance(students);
				} else if (selectNo == 5) {
					end();
					break;
				}
			}
		}
	}
}

```

### 도서

```jsx
package com.jeesung.exam01.today.test;

public class Student {
	private String name;
	private boolean checkedIn;
	
	public Student(String name) {
		super();
		this.name = name;
		this.checkedIn = false;
	}

	public void checkIn() {
		this.checkedIn = true;
	}
	
	public String getName() {
		return name;
	}

	public void setName(String name) {
		this.name = name;
	}

	public boolean isCheckedIn() {
		return checkedIn;
	}

	public void setCheckedIn(boolean checkedIn) {
		this.checkedIn = checkedIn;
	}
	
	public void printInfo() {
		System.out.println(name + " - 대출: " + (checkedIn ? "O" : "X"));
	}
	
	

}

```

### 메인

```jsx
package com.jeesung.exam01.today.test;

public class Main {
	public static void main(String[] args) {
		AttendanceSystem.start();
	}

}

```
