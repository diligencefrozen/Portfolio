# 숫자 야구

<aside>
💡

3자리 정수 (백의자리 숫자 0 가능), 중복 숫자 x

입력하는 부분

- 1. 숫자 아닌거 입력 못하게(값이 안넘어가게)
- 2. 중복 숫자 입력 => 다시 입력하게
- 3. 무조건 3자리 정수여야 하도록
- 4.유효성 검사!!
</aside>

## HomeController.java

```java
package com.beaver.may071.main;

import java.io.IOException;
import java.util.Random;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

// 숫자야구
// 3자리 정수 (백의자리 숫자 0 가능), 중복 숫자 x
// 입력하는 부분
//		1. 숫자 아닌거 입력 못하게(값이 안넘어가게)
//		2. 중복 숫자 입력 => 다시 입력하게
//		3. 무조건 3자리 정수여야 하도록
//		4.유효성 검사!!

@WebServlet("/HomeController")
public class HomeController extends HttpServlet {
	private static final long serialVersionUID = 1L;
	
	public static String pickAns() {
		int a = new Random().nextInt(976) + 12; // 12~987
		String ans = String.format("%03d", a); // "012" ~ "987"
		
		System.out.println(ans.charAt(0) == ans.charAt(1));
		//charAt() : 해당 인덱스에 있는 문자를 반환
		// char 자료형은 ascii (특정 정수값 ) 코드값으로 사용하기 떄문에
		
		if (ans.charAt(0) == ans.charAt(1) || ans.charAt(0) == ans.charAt(2) || ans.charAt(1) == ans.charAt(2)) {
			return pickAns();
		}
		return ans;
	}
       
    public HomeController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
//		HttpSession session = request.getSession();
//		session.setAttribute("target",  pickAns());
//		session.setAttribute("tryCount",  0);
//		session.setAttribute("result",  "Start.");
//		response.sendRedirect("index.jsp");
		if (!request.getParameterNames().hasMoreElements()) {
			NBGameEngine.getNge().pickAns();
		} else {
			NBGameEngine.getNge().doNB(request);
		}
		request.getRequestDispatcher("index.jsp").forward(request, response);
		
		

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
//		request.setCharacterEncoding("UTF-8");
//		String userNumbers = request.getParameter("userNumbers");
//		HttpSession session = request.getSession();
//		String target = (String) session.getAttribute("target");
//		if (target == null) target = pickAns();
//		
//		int strike = 0;
//		int ball = 0;
		request.setCharacterEncoding("UTF-8");
		NBGameEngine.getNge().doNB(request);
		request.getRequestDispatcher("index.jsp").forward(request, response);
		
		
		

	}

}

```

## index.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>숫자 야구 게임 모드</title>
<script type="text/javascript" src="js/BeaverValidChecker.js"></script>
<script type="text/javascript" src="js/NBValidChecker.js"></script>
<script type="text/javascript" src="js/go.js"></script>
<style>
	@charset "UTF-8";
	body { 
		text-align: center; 
	 	font-family: sans-serif; 
	 	background-color: #f4f4f4; 
	 }
	.vs-container { 
		display: flex; 
		justify-content: center; 
		align-items: center; 
		gap: 40px; 
		margin: 20px; 
		
	}
	img {
		width: 150px;
		height: 150px;
	
	}
	
	a {
		width: 150px;
		height: 150px;
		transition: transition 0.3s ease, opacity 0.3s;
	
	}
	
	a:hover {
		transform: scale(1,1);
		opacity: 0.244;
		cursor: pointer;
	}
	
	button {
		background-color: #bbb2e9;
		transition: transition 0.3s ease, opacity 0.3s;
	}
	
	button:hover {
		transform: scale(1,1);
		opacity: 0.344;
		cursor: pointer;
	}
	
	input[type="submit"] {
		padding: 10px 20px;
		font-size: 16px;
		background-color: #bbb2e9;
		color: white;
		border: none;
		cursor: pointer;
	}
	
	input[type="text"] {
		padding: 10px;
		font-size: 16px;
	}
	
	form {
		margin-top: 20px;
	}

	
</style>

</head>
<body>

    <h1 align="center">숫자 야구 게임 모드</h1>
    
    <form action="HomeController" method="post" align="center" name="nbForm" onsubmit="return check();">
    	<input type="text" name="userAns" maxlength="3" required placeholder='3자리' pattern="\d{3}">
    	<input type="submit" value="게임 시작">
    </form>
    
    <hr>
    
    <% if (request.getAttribute("t") != null) { %> 
        <div class="result-box">
            <h3><%= request.getAttribute("t") %></h3> 
            <p><%= request.getAttribute("ua") != null ? request.getAttribute("ua") : "" %></p> 
            
            <h2 style="color: blue;">
                <%= request.getAttribute("s") != null ? request.getAttribute("s") : "" %>
                <%= request.getAttribute("b") != null ? request.getAttribute("b") : "" %>
            </h2>

            <% if (request.getAttribute("r") != null) { %>
                <h1 style="color: red;"><%= request.getAttribute("r") %></h1>
                <div><%= request.getAttribute("btn") %></div> 
                <%= request.getAttribute("con") %> 
                <%= request.getAttribute("call") %> 
            <% } %>
        </div>
    <% } %>
    	
</body>
</html>
```

## NBGameEngine.java

```java
package com.beaver.may071.main;

import java.util.Iterator;
import java.util.Random;

import javax.servlet.http.HttpServletRequest;

public class NBGameEngine {
	
	private int t;
	private String ans;
	
	private static final NBGameEngine NGE = new NBGameEngine();
	
	public static NBGameEngine getNge() {
		return NGE;
	}
	
	public void pickAns() {
		int a = new Random().nextInt(976) + 12;
//		String ans = String.format("%03d", a);
		ans = String.format("%03d", a);
		if (ans.charAt(0) == ans.charAt(1) || ans.charAt(0) == ans.charAt(2) || ans.charAt(1) == ans.charAt(2)) {
			pickAns();
			return;
		}
		this.ans = ans;
	}
	
	public void doNB(HttpServletRequest request) {
		System.out.println(ans);
		String userAns = request.getParameter("userAns");
		request.setAttribute("ua", "유저의 답 :" +userAns);
		
		int strike = 0;
		int ball = 0;
		
		for (int i = 0; i < 3; i++) {
			for (int j = 0; j < 3; j++) {
				if (userAns.charAt(i) == ans.charAt(j)) {
					if (i == j) {
						strike++;
					} else {
						ball++;
					}
				}
			}
		}
		t++;
		request.setAttribute("t", t + "번째 시도!");
		request.setAttribute("s", "S : " + strike);
		request.setAttribute("b", "B : " + ball);
		
		if (strike == 3) {
			String result = "정답!" + ans + "이 었습니다!";
			t = 0;
			request.setAttribute("r", result);
			request.setAttribute("ua", null);
			request.setAttribute("s", null);
			request.setAttribute("b", null);
			
			request.setAttribute("btn",
					"<button onclick='goHome();'>다시 시작</button>");
			request.setAttribute("con",  "<script>alert('ㅊㅊ');</script>");
			request.setAttribute("call",
					"<img src='C:\\Users\\sdedu01\\Desktop\\WorkSpace\\JSP\\May06_2_RSP\\src\\main\\webapp\\image\\Computer.jpg'>");
		}
	}
	
	

}

```

## NBValidChecker.js

```jsx
/**
 * 
 */

function check() {
	let numBox = document.nbForm.userAns;
	if (isEmpty(numBox) 
		|| atLeastLetter(numBox, 3)
		|| isNotNumber(numBox)
		|| numBox.value[0] == numBox.value[1]
		|| numBox.value[0] == numBox.value[2]
		|| numBox.value[1] == numBox.value[2]) {
			
			alert("Please Fill in Number Properly.");
			numBox.value = "";
			numBox.focus();
			return false;
		}
		return true;
	
}
```

## go.js

```jsx
/**
 * 
 */

function goHome() {
	location.href = "HomeController";
}
```

## BeaverValidChecker.js

```jsx
/**
 * 
 */
// 잘못됐으면 true값
//(뭔가 문제가 되면 true, 정상적이면 false)

// <input> 을 넣었을 때
// 아무것도 안썼으면 true, 뭐라도 썼으면 false
function isEmpty(input) {
	return (!input.value);

}

// <input>을 넣었을떄
// 한글/ 특수문자 적혀있다면 true, 없다면 false
function containsAnother(input) {
	let pass = "1234567890zxcvbnmasdfghjklqwertyuiopZXCVBNMASDFGHJKLQWERTYUIOP@_.";
	let iv = input.value;
	for (let i = 0; i < iv.length; i++) {
		if (pass.indexOf(iv[i]) == -1) { // pass 에 있는 내용중에서 내가 쓴 내용이 없으면
			
			return true;
			}
	}
	return false;
}

// <input>, 글자 수를 넣었을 때
// 그 글자 수 보다 적으면 true, 그 글자수 이상이면 false
function atLeastLetter(input, len) {
	return (input.value.length < len);
}

// <input> x 2 넣었을 때
// 내용이 다르면 true, 같으면 false (pw, pw 확인)
function notEqualPw(input1, input2) {
	return (input1.value != input2.value);
}

// <input>, 문자열세트 넣었을 때
// 없으면 true, 있으면 false, (입력한 pw에 지정한 문자열이 없는 경우)
function notContain(input, passSet) {
	let iv = input.value;
	for (let i = 0; i < passSet.length; i++) {
		if (iv.indexOf(passSet[i]) != -1) {
			return false;
		}
	}
	return true;
}

// <input> 넣었을 때 
//숫자 아닌거 있으면 true, 숫자만 있으면 false
function isNotNumber(input) {
	return isNaN(input.value); 
}

// <input>, 확장자 넣었을 때
// 확장자명이 아니면 true, 맞으면 false
function isNotType(input, type) {
	// .png, .gif, .bmp, ...
	type = "." + type;
	return (input.value.inderOf(type) == -1);
}
```

## 여담 -Test.java

```java
package com.beaver.may071.main;

import java.util.Random;

public class Test {
	
	// 0 ~ 999
	// 1 ~ 11 
	// 12 ~ 987 => 정답이 될 수 있음 !!
	// 988 ~ 999
	public static String pickAns() {
		int a = new Random().nextInt(976) + 12; // 12~987
		String ans = String.format("%03d", a); // "012" ~ "987"
		
		System.out.println(ans.charAt(0) == ans.charAt(1));
		//charAt() : 해당 인덱스에 있는 문자를 반환
		// char 자료형은 ascii (특정 정수값 ) 코드값으로 사용하기 떄문에
		
		if (ans.charAt(0) == ans.charAt(1) || ans.charAt(0) == ans.charAt(2) || ans.charAt(1) == ans.charAt(2)) {
			return pickAns();
		}
		return ans;
	}

	public static void main(String[] args) {
		String ans = pickAns();
		System.out.println(ans);
	}
}

```

---

# 홀짝게임

<aside>
💡

홀/짝 이미지 눌러서 게임 한 판 진행

전적 확인 (0전 0승 0 패(승률 : 00.00%))

전적 초기화 (POST 방식 요청)

</aside>

## HomeController.java

```java
package com.beaver.may072.main;

import java.awt.Desktop.Action;
import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.jasper.tagplugins.jstl.core.If;

@WebServlet("/HomeController")
public class HomeController extends HttpServlet {
	private static final long serialVersionUID = 1L;
	// 홀짝게임
	// 홀/짝 이미지 눌러서 게임 한 판 진행
	// 전적 확인 (0전 0승 0 패(승률 : 00.00%))
	// 전적 초기화 (POST 방식 요청)
    public HomeController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		if (request.getParameterNames().hasMoreElements()) {
			Game.getGge().doGG(request);
			
		} else {
			request.setAttribute("total", "<< 홀이나 짝이냐 >>");
		}
		request.getRequestDispatcher("index.jsp").forward(request, response);	

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		String action = request.getParameter("action");
//		String userHand = request.getParameter("userHand");
//		
		if ("reset" .equals(action)) {
			Game.getGge().reset(request);
		} else {
			Game.getGge().doGG(request);
		}
		request.getRequestDispatcher("index.jsp").forward(request, response);
		
//		Game.getGge().reset(request);
//		request.getRequestDispatcher("index.jsp").forward(request, response);	
		
		
		
	}
	
}

```

## index.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>홀짝 게임</title>

</head>
<body>

    <h1 align="center">홀짝 게임</h1>
    <div id="result" align="center">
    <h2>결과: ${r != null ? r: '홀 또는 짝을 선택.'}</h2>
    <h3>전적: ${total !=  null ? total :0}
    	${win !=  null ? win :0}
   		${lose !=  null ? lose :0}</h3>
   	<h4>(승률: ${p != null ? p : '0.00'}%)</h4>
   	</div>
   	<!--  
   	<table align="center">
   		<tr>
   			<th>총 판수</th>
   			<th>승</th>
   			<th>패</th>
   		</tr>
   		
   		<tr>
   		<td align="center">${w != null ? w : 0}</td>
   		<td align="center">${win !=  null ? win :0}</td>
   		<td align="center">${lose !=  null ? lose :0}</td>
   		
   	</table>
   	-->
    <form action="HomeController" method="post" align="center" name="nbForm" onsubmit="return check();">
    	<table align="center" border="1">
    		<tr>
    			<td align="center"> 
    				<button type="submit" name="UserAns" value="1" style="background: none; border: none; cursor: pointer;">
    					<img src="img/hol.png" width="150" alt="홀">
    					<p>홀수</p>
    				</button>
    			</td>
    			
    			<td align="center"><h1>VS</h1></td>
    			
    			
    			<td align="center">
    				<button type="submit" name="UserAns" value="0" style="background: none; border: none; cursor: pointer;">
    					<img src="img/JJAK.png" width="150" alt="짝">
    					<p>짝수</p>
    				</button>
    			</td>
    		</tr>
    		
    		<tr>
    			<td colspan="3" align="center">
    				<button type="submit" name="action" value="reset" style="width: 100%; height: 40px; margin-top: 10px; cursor: pointer;">
    				그레이트 리셋
    				</button>
    			</td>
    		</tr>
    			
    	</table>
    </form>

</body>
</html>

```

## Game.java

```java
package com.beaver.may072.main;

import java.util.Random;

import javax.servlet.http.HttpServletRequest;

public class Game {
	private static int total = 0;
	private static int win = 0;
	private static int lose = 0;
	private String pp; // 승률
	
	public static final Game GGE = new Game();
	
	public static Game getGge() {
		return GGE;
		
	}

	////////////////////
	
	public static int getTotal() {
		return total;
	}

	public static void setTotal(int total) {
		Game.total = total;
	}

	public static int getWin() {
		return win;
	}

	public static void setWin(int win) {
		Game.win = win;
	}

	public static int getLose() {
		return lose;
	}

	public static void setLose(int lose) {
		Game.lose = lose;
	}

	public String getPp() {
		return pp;
	}

	public void setPp(String pp) {
		this.pp = pp;
	}
	
	///////////////////////////////////
	
	public void doGG(HttpServletRequest request) {
		int userAns = Integer.parseInt(request.getParameter("UserAns"));
		int coin = new Random().nextInt(10) +1;
		request.setAttribute("c", coin + "개");
		
		String gameAns = (coin % 2 == 0) ? "짝" : "홀";
		request.setAttribute("r", gameAns);
		
		if ((userAns) == (coin%2)) {
			win++;
		} else {
			lose++;
		}
		total++;
		
		request.setAttribute("total", total + "전");
		request.setAttribute("win", win + "승");
		request.setAttribute("lose", lose + "패");
		double p = (win / (double) total) * 100;
		request.setAttribute("total", total + "전");
		pp = String.format("%.2f", p);
//		request.setAttribute("p", "(승률 : " + pp + "%)");
		request.setAttribute("p", pp);
	}
	
	public void reset(HttpServletRequest request) {
		total = 0;
		win = 0;
		lose = 0;
		pp = "0";
		
		request.setAttribute("total", total + "전");
		request.setAttribute("win", win + "승");
		request.setAttribute("lose", lose + "패");
//		request.setAttribute("p", "(승률 : " + pp + "%)");
		request.setAttribute("p", pp);
		request.setAttribute("r", "초기화 완료");
		
	}
	
//	public void doGame(HttpServletRequest request) {
//		int userHand = Integer.parseInt(request.getParameter("userHand"));
//		request.setAttribute("uh",  "image/" + userHand + ".png");
//		
//		int comHand = new Random().nextInt(10) + 1;
//		request.setAttribute("ch",  "image/" + comHand + ".png");
//		
//		int result = userHand - comHand;
//		if (result == 0) {
//			request.setAttribute("r", "무");
//		}
//		
//	}

}

```
