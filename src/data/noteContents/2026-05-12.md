# HomeController.java

```java
package com.beaver.may111.main;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.beaver.may111.apple.AppleDAO;

@WebServlet("/HomeController")
public class HomeController extends HttpServlet {
	private static final long serialVersionUID = 1L;
	//제목
	//메뉴 4개 - 다른 페이지로 이동할수 있는
	//보여줄 내용 1개
       
	// 톰캣이 이 프로젝트를 처음 실행시킬때
    public HomeController() {
        super();
        // TODO Auto-generated constructor stub
        AppleDAO.getAppledao().countApples();
        // 사과데이터 총 갯수 가져오기
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
//		String page = request.getParameter("page");
//		String contentPage = "menu.jsp";
//		
//		if (page != null && page.equals("m2")) {
//			contentPage = "menu02.jsp";
//		}
//		AppleDAO.getAppledao().getAllApples(request);
		
		String pg = request.getParameter("pg");
		String contentPage = "menu.jsp";
		
		if (pg == null || pg.equals("m1")) {
			AppleDAO.getAppledao().getApples(1, request);
			contentPage = "menu.jsp";
		} else if (pg.equals("m2")) {
			contentPage = "menu02.jsp";
		} else if (pg.equals("m3")) {
			contentPage = "menu03.jsp";
		}
		
//		request.setAttribute("cp",  "menu.jsp");
//		request.setAttribute("cp",  "menu02.jsp");
//		request.setAttribute("cp",  "mene03.jsp");
//		request.setAttribute("cp",  "menu04.jsp");
		
//	    if (pg != null) {
//	        if (pg.equals("m2")) { contentPage = "menu02.jsp"; }
//	        else if (pg.equals("m3")) { contentPage = "menu03.jsp"; }
//	    }
	    
		request.setAttribute("cp", contentPage);
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
//		request.setCharacterEncoding("UTF-8");
//		
//		String action = request.getParameter("action");
//		String viewPage = "";
//		
//		if (action == null) {
//			viewPage = "/index.jsp";
//		} else if (action.equals("menu")) {
//			viewPage = "/menu.jsp";
//		} else if (action.equals("menu02")) {
//			viewPage = "/menu02.jsp";
//		} else if (action.equals("menu03")) {
//			viewPage = "/menu03.jsp";
//		} else if (action.equals("menu04")) {
//			viewPage = "/menu04.jsp";
//		}
//		request.getRequestDispatcher(viewPage).forward(request, response);

	}

}

```

# index.jsp

<!--<jsp:include page="${cp}" /> →

→ 이 부분은 HTML 주석이지만, JSP 에서는 실행되기 때문에 오류가 발생한다.

따라서, HTML 주석용 태그가 아니라, JSP 주석용 태그를 사용하면, 문제가 해결된다.

<%--<jsp:include page="${cp}" />--%>

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Index</title>
<link rel="stylesheet" href="index.css">
</head>
<body>

	<h1 id="parent">제목</h1>
	<table id="child">
		<tr>
			<td>
				Test
			</td>
		</tr>
	</table>
	<img id = "fruit" src="img/fruit.jpg" >
	<div id ="menuarea"> <!-- div로 감싸주면 , css로 대응할수 있다. (가로로 나열하려면) -->
	<!--  <table id="child">-->

				<form action="AppleController" method="get">
					<!-- <input type="hidden" name="action" value="menu"> -->
					<input type="hidden" name="pg" value="m1">

					<button type="submit">>Apple</button>
				</form>

				<form action="HomeController" method="get">
					<input type="hidden" name="pg" value="m2">
				    <%--<jsp:include page="${cp}" />--%>
					<button type="submit">>menu02</button>
				</form>

				<form action="HomeController" method="get">
					<input type="hidden" name="pg" value="m3">

					<button type="submit">>menu03</button>
				</form>

				<form action="DBCController" method="get">
					<input type="hidden" name="pg" value="m4">

					<button type="submit">>DB연결</button>
				</form>
			</div>

	
	<div id ="contentArea">
		<c:if test="${not empty cp }">
			<jsp:include page="${cp }" />
		</c:if>
	</div>

</body>
</html>
```

# AppleDAO.java

```java
package com.beaver.may111.apple;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.util.ArrayList;

import javax.servlet.http.HttpServletRequest;

import com.beaver.db.manager.BeaverDBManager;

public class AppleDAO {
	private static final AppleDAO APPLEDAO = new AppleDAO();
	private int allAppleCount;
	public static final AppleDAO getAppledao() {
		return APPLEDAO;
	}
	
	// 모든 사과 데이터 다 가져오기 
	public void getAllApples(HttpServletRequest req) {
		Connection con = null;
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		
		try {
			con = BeaverDBManager.connect("beaverPool");
			// SQL 문 작성
			String sql = "select * from menu order by menu_price";
			pstmt = con.prepareStatement(sql);
			
			// C / U / D : pstmt.executeUpdate()로 실행
			//				=> 데이터 몇 개 영향을 받았는지 
			// R : pstmt.executeQuery()로 실행
			//		=>select 결과를 ResultSet으로 보냄
			rs = pstmt.executeQuery();
			
			ArrayList<Apple> apples = new ArrayList<Apple>();
			Apple apple = null;
			while (rs.next()) {
				apple = new Apple();
				apple.setMenu_location(rs.getString("menu_location"));
				apple.setMenu_colour(rs.getString("menu_colour"));
				apple.setMenu_taste(rs.getString("menu_taste"));
				apple.setMenu_price(rs.getInt("menu_price"));
				apple.setMenu_desc(rs.getString("menu_desc"));
				apples.add(apple);
			}
			req.setAttribute("apples", apples);
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
		}
		BeaverDBManager.close(con, pstmt, rs);
	}
	
	
	// 등록하기
	public void reg(HttpServletRequest req) {
		Connection con = null;
		PreparedStatement pstmt = null;
		try {
			con = BeaverDBManager.connect("beaverPool");
			
			// 데이터 입력
			req.setCharacterEncoding("UTF-8");
			String location = req.getParameter("menu_location");
			String colour = req.getParameter("menu_colour");
			String taste = req.getParameter("menu_taste");
			int price = Integer.parseInt(req.getParameter("menu_price"));
			
			// <textarea>에서 엔터친거 : \r\n
			// 웹 페이지에서 줄 바꾸기 : <br>
			String desc = req.getParameter("menu_desc").replace("\r\n","<br>");
			
			
			// sql문 작성
			String sql = "insert into menu values(?, ?, ?, ?, ?)";
			
			// DB 관련 총괄매니저 (pstmt)
			pstmt = con.prepareStatement(sql);
			
			// sql 완성
			pstmt.setString(1, location);
			pstmt.setString(2, colour);
			pstmt.setString(3, taste);
			pstmt.setInt(4, price);
			pstmt.setString(5, desc);
			
			// 실행 + 결과 처리
			if (pstmt.executeUpdate() == 1) {
				req.setAttribute("r", "[등록성공]");
				allAppleCount++;
				
			}
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
			req.setAttribute("r", "[등록실패]");
//			allAppleCount++;
		}
		BeaverDBManager.close(con, pstmt, null);
	}
	
	// 총 데이터가 몇 개 인지
	public void countApples() {
		Connection con = null;
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		try {
			con = BeaverDBManager.connect("beaverPool");
			String sql = "select count(*) from menu";
			pstmt = con.prepareStatement(sql);
			rs = pstmt.executeQuery();
			rs.next();
			allAppleCount = rs.getInt("count(*)");
			System.out.println(allAppleCount);
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
		}
		BeaverDBManager.close(con, pstmt, rs);
	}
	
	// DB 에 데이터가 100개 있다 ? = > 해당 페이지 번호에 맞는 데이터만 가져오기
	public void getApples(int pageNo, HttpServletRequest req) {
		Connection con = null;
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		try {
			con = BeaverDBManager.connect("beaverPool");
			
			// SQL 수행하는데 필요한 값 챙기기 
			
			// allAppleCount : 전체 사과 데이터 수
			int applePerPage = 4; // 한 페이지당 보여줄 사과 데이터 수
			
			// 페이지 갯수 가져오기
			// Math.ceil : 숫자  올림 (반올림 X)
			int pageCount = (int) Math.ceil(allAppleCount / (double) applePerPage);
			req.setAttribute("pageCount", pageCount);
			///////////////////////////////////////////
			
			int start = (applePerPage * (pageNo - 1)) + 1;
			int end = (pageNo == pageCount) ? allAppleCount : (start + applePerPage - 1);
			
			String sql = "select * from (select rownum as rn, menu_location, menu_colour, menu_taste, menu_price, menu_desc from (select * from menu order by menu_price)) where rn >= ? and rn <= ?";
			
			pstmt = con.prepareStatement(sql);
			pstmt.setInt(1,  start);
			pstmt.setInt(2,  end);
			rs = pstmt.executeQuery();
			
			ArrayList<Apple> apples = new ArrayList<Apple>();
			Apple apple = null;
			while (rs.next()) {
				apple = new Apple();
				apple.setMenu_location(rs.getString("menu_location"));
				apple.setMenu_colour(rs.getString("menu_colour"));
				apple.setMenu_taste(rs.getString("menu_taste"));
				apple.setMenu_price(rs.getInt("menu_price"));
				apple.setMenu_desc(rs.getString("menu_desc"));
				apples.add(apple);
				
			}
			req.setAttribute("apples", apples);
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
		}
		BeaverDBManager.close(con, pstmt, rs);
	}
	
	// 상세정보 가져오기 (Primary Key)
	// 제대로 데이터를 가져왔으면 : true
	// 실패했으면 : false
	public boolean getAppleDetail(HttpServletRequest req) {
		Connection con = null;
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		try {
			con = BeaverDBManager.connect("beaverPool");
			
			String location = req.getParameter("menu_location");
			String sql = "select * from menu where menu_location =?";
			pstmt = con.prepareStatement(sql);
			pstmt.setString(1, location);
			rs = pstmt.executeQuery();
			
			if (rs.next()) {
				Apple apple = new Apple();
				apple.setMenu_location(rs.getString("menu_location"));
				apple.setMenu_colour(rs.getString("menu_colour"));
				apple.setMenu_taste(rs.getString("menu_taste"));
				apple.setMenu_price(rs.getInt("menu_price"));
				apple.setMenu_desc(rs.getString("menu_desc"));
				req.setAttribute("apple", apple);
				return true; // 일반적인 상황 : 데이터가 하나
			}
			return false; // ex) 다른 사람이 그 데이터를 지웠으면
							// : 데이터 없음
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
			return false; // DB 서버 상태가 안좋음(서버 터짐)
		} finally {
			BeaverDBManager.close(con, pstmt, rs);
		}
	}
	
    public boolean update(HttpServletRequest req) {
    	Connection con = null;
    	PreparedStatement pstmt = null;
    	try {
			con = BeaverDBManager.connect("beaverPool");
			req.setCharacterEncoding("UTF-8");
			String location = req.getParameter("menu_location");
			String colour = req.getParameter("menu_colour");
			String taste = req.getParameter("menu_taste");
			int price = Integer.parseInt(req.getParameter("menu_price"));
			String desc = req.getParameter("menu_desc");
			String sql = "update menu set menu_colour=?, menu_taste=?, menu_price=?, menu_desc=? where menu_location=?";
			
			pstmt = con.prepareStatement(sql);
			pstmt.setString(1, colour);
			pstmt.setString(2, taste);
			pstmt.setInt(3, price);
			pstmt.setString(4, desc);
			pstmt.setString(5, location);
			
			if (pstmt.executeUpdate() == 1) {
				req.setAttribute("r", "[수정 성공]");
				return true;
			} else {
				req.setAttribute("r", "[수정 실패]");
				return false;
			}
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
			req.setAttribute("r", "[수정 실패]");
			return false;
		} finally {
			BeaverDBManager.close(con, pstmt, null);
		}
    }
    
    // 데이터 삭제
    public void delete(HttpServletRequest req) {
    	Connection con = null;
    	PreparedStatement pstmt = null;
    	try {
			con = BeaverDBManager.connect("beaverPool");
			String location = req.getParameter("menu_location");
			System.out.println("삭제할 location=" +location);
			String sql = "delete from menu where menu_location = ?";
			pstmt = con.prepareStatement(sql);
			pstmt.setString(1, location);
			int result = pstmt.executeUpdate();
			System.out.println("삭제 결과 = " + result);
			
			if (pstmt.executeUpdate() == 1) {
				req.setAttribute("r", "[삭제 성공]");
				allAppleCount--;
			} else {
				req.setAttribute("r", "[삭제 실패]");
			}
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
			req.setAttribute("r", "[삭제 실패]");
		}
    	BeaverDBManager.close(con, pstmt, null);
    }

}

```

# menu.jsp

```java
<%@page import="java.sql.ResultSet"%>
<%@page import="java.sql.PreparedStatement"%>
<%@page import="com.beaver.db.manager.BeaverDBManager"%>
<%@page import="java.sql.Connection"%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Menu</title>
<link rel="stylesheet" href="index.css">
<script type="text/javascript" src="js/BeaverValidChecker.js"></script>
<script type="text/javascript" src="js/May12Check.js"></script>
<script type="text/javascript" src="js/go.js"></script>
</head>
<body>
	<h1 id="parent">Menu</h1>
	
		<span id="appleTbl">${r }</span>
		<form action="AppleregController" method="post" align="center" name="appleRegForm" onsubmit="return appleRegCheck();">
		<table id="regTbl">
		<tr>
			<td>
				지역
			</td>
			
			<td>
				<input type="text" name="menu_location" class="input-field" required>
			</td>
			
			<td>
				색
			</td>
			
			<td>
				<select name="menu_colour" class="input-field" required>
					<option value="">선택</option>
					<option value="검정">검정</option>
					<option value="흰색">흰색</option>
					<option value="녹색">녹색</option>
					<option value="보라색">보라색</option>
					<option value="회색">회색</option>
					<option value="주황색">주황색</option>
					<option value="분홍색">분홍색</option>
					<option value="갈색">갈색</option>
					<option value="파란색">파란색</option>
				</select>
			</td>
			
			<td>
				맛
			</td>
			
			<td>
				<select name="menu_taste" class="input-field" required>
					<option value="">선택</option>
					<option value="단맛">단맛</option>
					<option value="쓴맛">쓴맛</option>
					<option value="신맛">신맛</option>
					<option value="짠맛">짠맛</option>
				</select>
			</td>
			
			<td>
				가격
			</td>
			
			<td>
				<input type="text" name="menu_price" class="input-field" required>
			</td>
			
			<td>
				소개
			</td>
			
			<td>
				<input type="text" name="menu_desc" class="input-field" required>
			</td>
			<td align="center">
				<!-- <a href="AppleregController">등록</a> -->
				<button type="submit">등록</button>
			</td>
		</tr>

		</table>
	</form>
		
		
		<table class="datatable">
		<thead>
			<tr>
				<td>지역</td>
				<td>가격</td>
			</tr>
		</thead>
		<tbody>
		<c:forEach var="apple" items='${apples}'>  
			<tr class ="dataTr" onclick="goAppleDetail('${apple.menu_location}');">
				<td>${apple.menu_location}</td>

				<td>
					<fmt:formatNumber value="${apple.menu_price}" type="currency" />
					

				</td>
				
			</tr>
		</c:forEach>
		</tbody>
	</table>
	<div class="pagingArea">
		<tr>
			<td align="center">
				<c:forEach var="p" begin="1" end="${pageCount }">
					<a href="ApplePageController?p=${p }">${p }</a>
				</c:forEach>
			</td>
		</tr>
	</div>
	
	<% 
	// 기존:DriverManager.getConnection()을 실행하는 순간
	// 관련된 모든 작업을 해서 Connection 객체를 만들어줌
	// 커넥션 풀: 미리 Connection 객체 x 100개 만들어놓고
	// 클라이언트가 요청하면 => 만들어진거 하나 주면 됨
	
	// 설정 - context.xml(설계도, 사업계획서 느낌)
	// Server - contaxt.xml
	// 저걸 건들면.. 워크스페이스 안에 있는 
	// 모든 프로젝트에 다 영향을 줌
	// 프로젝트 가 다르다 =>DB 서버가 다르다
	// 우리가 만든 context.xml을 각 프로젝트의 META-INF 폴더에 넣자.
	%>

</body>
</html>
```

# menu02.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Menu2</title>
<link rel="stylesheet" href="index.css">
</head>
<body>
	<h1 id="parent">Menu2</h1>
	<table id="child">
		<tr>
			<td>
				내용
			</td>
		</tr>
	</table>

</body>
</html>
```

# menu03.jsp

```java
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Menu3</title>
<link rel="stylesheet" href="index.css">
</head>
<body>
	<h1 id="parent">Menu3</h1>
	<table id="child">
		<tr>
			<td>
				내용
			</td>
		</tr>
	</table>

</body>
</html>
```

# menu04.jsp

```java
<%@page import="com.beaver.db.manager.BeaverDBManager"%>
<%@page import="java.sql.Connection"%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Menu4</title>
<link rel="stylesheet" href="index.css">
</head>
<body>
	<h1 id="parent">DB 연결에 대한 정보를 불러옵니다</h1>
	<table id="child">
		<tr>
			<td>
				내용
			</td>
		</tr>
	</table>
	<% 
	// 기존:DriverManager.getConnection()을 실행하는 순간
	// 관련된 모든 작업을 해서 Connection 객체를 만들어줌
	// 커넥션 풀: 미리 Connection 객체 x 100개 만들어놓고
	// 클라이언트가 요청하면 => 만들어진거 하나 주면 됨
	
	// 설정 - context.xml(설계도, 사업계획서 느낌)
	// Server - contaxt.xml
	// 저걸 건들면.. 워크스페이스 안에 있는 
	// 모든 프로젝트에 다 영향을 줌
	// 프로젝트 가 다르다 =>DB 서버가 다르다
	// 우리가 만든 context.xml을 각 프로젝트의 META-INF 폴더에 넣자.
	String result = "실패";
	Connection con = null;
	try {
		con = BeaverDBManager.connect("beaverPool"); 
		result = "성공";
	} catch (Exception e) {
		e.printStackTrace();
	}
	
	%>
	
	<h1 id="menu04Tb">DB연결</h1>
	<h1 id="menu04Tb_2"><%=result %></h1>

</body>
</html>
```

# index.css

```css
@charset "UTF-8";

#parent, #regTbl, .appleRegForm {
	display: grid;
	place-items: center;
	height: 100px;
	border: 1px solid black;
	
}

#child, #child02, #child03, #child04 {
	display: grid;
	place-items: center;
	height: 50px;
	
}

#contentArea {
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: 30px;

}

#fruit {
	place-items: center;
	display: block;
	margin: 0 auto;
}

#menu04Tb, #menu04Tb_2 {
	place-items: center;
	display: grid;
	border: 1;
}

#menuarea {
	display: flex;
	justify-content: center;
	align-items: center;
	gap: 30px;
	margin: 30px 0;
		
}

#insert {
	display: flex;
	justify-content: center;
	align-items: center;
	gap: 20px;
	margin: 20px 0;	
	
}
```

# db.sql

```sql
-- menu1 테이블 : 지역 / 색 / 맛 / 가격 / 소개정보
create table menu( 
	menu_location varchar2(10 char) primary key,
	menu_colour varchar2(10 char) not null,
	menu_taste varchar2(10 char) not null,
	menu_price number(20) not null,
	menu_desc varchar2(200 char) not null
);

INSERT INTO MENU VALUES('충주', '빨강','단맛',4000,'충주사과');
INSERT INTO MENU VALUES('성주', '녹색','신맛',3000,'충주사과');
SELECT * FROM MENU;

-- 테이블 생성 후 데이터 2개 정도 insert

-- SQL은 실행하려면 alt + x

```

# AppleController.java

```java
package com.beaver.may111.apple;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/AppleController")
public class AppleController extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    public AppleController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		request.setCharacterEncoding("UTF-8");
		AppleDAO.getAppledao().getAllApples(request);
		request.setAttribute("cp",  "menu.jsp");
		request.getRequestDispatcher("index.jsp").forward(request, response); // index.jsp 라고 적으면 오류남. menu.jsp에만 DB 정보를 호출하겠다는 의미인데.. 나중에 homecontroller 에서 실행하고싶다면 index.jsp로 변경
	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}

```

# AppleregController.java

```java
package com.beaver.may111.main;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.beaver.may111.apple.AppleDAO;

@WebServlet("/AppleregController")
public class AppleregController extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    public AppleregController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		request.setCharacterEncoding("UTF-8");
		AppleDAO.getAppledao().getAllApples(request);
		request.setAttribute("cp",  "menu.jsp");
		request.getRequestDispatcher("index.jsp").forward(request, response); // index.jsp 라고 적으면 오류남. menu.jsp에만 DB 정보를 호출하겠다는 의미인데.. 나중에 homecontroller 에서 실행하고싶다면 index.jsp로 변경
	}

	//등록하기
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		request.setCharacterEncoding("UTF-8");
		//DB에 등록
		AppleDAO.getAppledao().reg(request);
		
		AppleDAO.getAppledao().getApples(1, request);
		// 게시판에 나올 데이터 가지고
		AppleDAO.getAppledao().getAllApples(request);
		
		// 등록 후에 보여줄 페이지 이동
		request.setAttribute("cp", "menu.jsp");
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

}

```

# BeaverValidCheck.js

```jsx
/**
 *
 */
// 잘못됐으면 true값
//(뭔가 문제가 되면 true, 정상적이면 false)

// <input> 을 넣었을 때
// 아무것도 안썼으면 true, 뭐라도 썼으면 false
function isEmpty(input) {
	return (!input.value);

}

// <input>을 넣었을떄
// 한글/ 특수문자 적혀있다면 true, 없다면 false
function containsAnother(input) {
	let pass = "1234567890zxcvbnmasdfghjklqwertyuiopZXCVBNMASDFGHJKLQWERTYUIOP@_.";
	let iv = input.value;
	for (let i = 0; i < iv.length; i++) {
		if (pass.indexOf(iv[i]) == -1) { // pass 에 있는 내용중에서 내가 쓴 내용이 없으면

			return true;
			}
	}
	return false;
}

// <input>, 글자 수를 넣었을 때
// 그 글자 수 보다 적으면 true, 그 글자수 이상이면 false
function atLeastLetter(input, len) {
	return (input.value.length < len);
}

// <input> x 2 넣었을 때
// 내용이 다르면 true, 같으면 false (pw, pw 확인)
function notEqualPw(input1, input2) {
	return (input1.value != input2.value);
}

// <input>, 문자열세트 넣었을 때
// 없으면 true, 있으면 false, (입력한 pw에 지정한 문자열이 없는 경우)
function notContain(input, passSet) {
	let iv = input.value;
	for (let i = 0; i < passSet.length; i++) {
		if (iv.indexOf(passSet[i]) != -1) {
			return false;
		}
	}
	return true;
}

// <input> 넣었을 때
//숫자 아닌거 있으면 true, 숫자만 있으면 false
function isNotNumber(input) {
	return isNaN(input.value);
}

// <input>, 확장자 넣었을 때
// 확장자명이 아니면 true, 맞으면 false
function isNotType(input, type) {
	// .png, .gif, .bmp, ...
	type = "." + type;
	return (input.value.indexOf(type) == -1);
}
```

# may12Check.js

```jsx
/**
 * 
 */
function appleRegCheck() {
	let lBox = document.appleRegForm.menu_location;
	let pBox = document.appleRegForm.menu_price;
	let iBox = document.appleRegForm.menu_desc;
	
	if (isEmpty(lBox)) {
		alert("Please Fill in Location Properly.");
		lBox.value = "";
		lBox.focus();
		return false;
	} else if (isEmpty(pBox) || isNotNumber(pBox)) {
		alert("please Fill in Price preperly");
		pBox.value = "";
		pBox.focus();
		return false;
	} else if (isEmpty(iBox) || atLeastLetter(iBox, 5)) {
		alert("please Fill in Desc preperly");
		iBox.value();
		iBox.focus();
		return false;
	}
	return true;
}
```

# go.js

```jsx
/**
 * 
 */
function goAppleDetail(loc) {
	//alert(loc);
	location.href = "AppleDetailController?menu_location=" + encodeURIComponent(loc);
}

function deleteMenu(loc) {
	let t = confirm(`정말 삭제하시겠습니까?`);
	if (t) {
		location.href = "AppleDeleteController?menu_location=" + encodeURIComponent(loc);
	}
}
```

# ApplePageController.jave

```java
package com.beaver.may111.apple;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/ApplePageController")
public class ApplePageController extends HttpServlet {
	private static final long serialVersionUID = 1L;
       

    public ApplePageController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		int p = Integer.parseInt(request.getParameter("p"));
		AppleDAO.getAppledao().getApples(p, request);
		request.setAttribute("cp", "menu.jsp");
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}

```

# AppleDetailController.java

```java
package com.beaver.may111.apple;

import java.io.IOException;
import java.sql.Connection;
import java.sql.PreparedStatement;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.beaver.db.manager.BeaverDBManager;

@WebServlet("/AppleDetailController")
public class AppleDetailController extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    public AppleDetailController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		if (AppleDAO.getAppledao().getAppleDetail(request)) {
			request.setAttribute("cp",  "detail.jsp");
		} else {
			AppleDAO.getAppledao().getApples(1, request);
			request.setAttribute("cp",  "menu.jsp");
		}
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		// Update 
		if (AppleDAO.getAppledao().update(request)) {
			AppleDAO.getAppledao().getApples(1, request);
			request.setAttribute("cp", "menu.jsp");
		} else {
			AppleDAO.getAppledao().getApples(1, request);
			request.setAttribute("cp", "menu.jsp");
		}
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

}

```

# detail.jsp

```java
<%@page import="java.sql.ResultSet"%>
<%@page import="java.sql.PreparedStatement"%>
<%@page import="com.beaver.db.manager.BeaverDBManager"%>
<%@page import="java.sql.Connection"%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<%@ taglib prefix="fmt" uri="http://java.sun.com/jsp/jstl/fmt" %>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>수정</title>
<link rel="stylesheet" href="index.css">
<script type="text/javascript" src="js/BeaverValidChecker.js"></script>
<script type="text/javascript" src="js/May12Check.js"></script>
<script type="text/javascript" src="js/go.js"></script>
</head>
<body>
	<h1 id="parent">수정</h1>
	
		
		<table id="regTbl">
		<form action="AppleDetailController" method="post">
		<tr>
			<td>
				지역
				<input name="menu_location" value="${apple.menu_location}" readonly>
			</td>
			
			<td>
				색
				<input name="menu_colour" value="${apple.menu_colour}">
			</td>
			
			<td>
				맛
				<input name="menu_taste" value="${apple.menu_taste}">
			</td>
			
			<td>
				가격
				<input name="menu_price" value="${apple.menu_price}">
			</td>
			
			<td>
				소개
				<textarea name="menu_desc">${apple.menu_desc}</textarea>
			</td>
			<td align="center">
				<!-- <a href="AppleregController">등록</a> -->
				<button type="submit">수정</button>
			</form>
			
				<button type="button" onclick="deleteMenu('${apple.menu_location}');">삭제</button>
				<button type="button" onclick="history.back();">뒤로가기</button>
			</td>
		</tr>
	</table>
	
	
	<% 
	// 기존:DriverManager.getConnection()을 실행하는 순간
	// 관련된 모든 작업을 해서 Connection 객체를 만들어줌
	// 커넥션 풀: 미리 Connection 객체 x 100개 만들어놓고
	// 클라이언트가 요청하면 => 만들어진거 하나 주면 됨
	
	// 설정 - context.xml(설계도, 사업계획서 느낌)
	// Server - contaxt.xml
	// 저걸 건들면.. 워크스페이스 안에 있는 
	// 모든 프로젝트에 다 영향을 줌
	// 프로젝트 가 다르다 =>DB 서버가 다르다
	// 우리가 만든 context.xml을 각 프로젝트의 META-INF 폴더에 넣자.
	%>

</body>
</html>s
```

# AppleDeleteController.java

```java
package com.beaver.may111.apple;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/AppleDeleteController")
public class AppleDeleteController extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    public AppleDeleteController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		AppleDAO.getAppledao().delete(request);
		AppleDAO.getAppledao().getApples(1, request);
		request.setAttribute("cp",  "menu.jsp");
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}

```

# Apple.java

```java
package com.beaver.may111.apple;

// DB 컬럼명 = Java 멤버변수명 = 요청파라미터명
public class Apple {
	private String menu_location;
	private String menu_colour;
	private String menu_taste;
	private int menu_price;
	private String menu_desc;
	
	public Apple(String menu_location, String menu_colour, String menu_taste, int menu_price, String menu_desc) {
		super();
		this.menu_location = menu_location;
		this.menu_colour = menu_colour;
		this.menu_taste = menu_taste;
		this.menu_price = menu_price;
		this.menu_desc = menu_desc;
	}

	public Apple() {
		// TODO Auto-generated constructor stub
	}

	public String getMenu_location() {
		return menu_location;
	}

	public void setMenu_location(String menu_location) {
		this.menu_location = menu_location;
	}

	public String getMenu_colour() {
		return menu_colour;
	}

	public void setMenu_colour(String menu_colour) {
		this.menu_colour = menu_colour;
	}

	public String getMenu_taste() {
		return menu_taste;
	}

	public void setMenu_taste(String menu_taste) {
		this.menu_taste = menu_taste;
	}

	public int getMenu_price() {
		return menu_price;
	}

	public void setMenu_price(int menu_price) {
		this.menu_price = menu_price;
	}

	public String getMenu_desc() {
		return menu_desc;
	}

	public void setMenu_desc(String menu_desc) {
		this.menu_desc = menu_desc;
	}
	
	
	
	
	
	
	
	

}

```

# BeaverDBManager.java

```java
package com.beaver.db.manager;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

import javax.naming.Context;
import javax.naming.InitialContext;
import javax.sql.DataSource;

// 기존 JDBC 프로그램 구현 => DBMS와 연동을 작업
// 클라이언트로부터 요청이 있을 때마다 DB서버와 연결하기 위해서
// Connection 객체를 얻어내야 했음

// 단점
// 1. Connection 과정은 일정 시간이 필요한 작업
// 2. 불필요한 연결에 의해 서버 자원을 낭비

//단점 보완 => DBCP(DataBase Connection Pool)
// 여러개의 DB Connection을 하나의 Pool에 모아놓고 관리
// 필요할떄마다 Pool에서 불러다가 사용하면 O!
// 만약, 빌려올 수 있는 Connection이 없다면,
//		Connection 객체가 반환될 때까지 대기 상태로 전환
// 사용이 끝난 Connection 객체는 다시 Pool로 반환(.close())

// JNDI (Java Naming and Directory Interface)
// :Java 기술로 지정된 API로 어떤 파일/프로그램에 이름 지정 및 디렉토리 지정이 가능함
// InitialContext() : 이름이 있는 객체를 찾을 때 사용하는 객체
// DataSource : 커넥션 풀의 Connection을 관리하기 위한 객체
//		lookup 메소드를 통해서 관리, Connection 객체를 가져올 수 있음.

public class BeaverDBManager {
	public static Connection connect(String poolName) throws Exception {
		Context ctx = new InitialContext();
		DataSource ds = (DataSource) ctx.lookup("java:comp/env/" + poolName);
		//java:comp/env/ 는 context.xml 파일이 있는 경로
		return ds.getConnection();
	}
	
	public static void close(Connection con, 
							PreparedStatement pstmt,
							ResultSet rs) {
		try {
			rs.close();
		} catch (Exception e) {
			// TODO: handle exception
			
		}
		
		try {
			pstmt.close();
		} catch (Exception e) {
			// TODO: handle exception
			
		}
		
		try {
			con.close();
		} catch (Exception e) {
			// TODO: handle exception
			
		}
	}

}

```

# context.xml

```xml
<?xml version="1.0" encoding="UTF-8"?>
<Context>
	<Resource
		name="beaverPool"
		type="javax.sql.DataSource"
		driverClassName="oracle.jdbc.driver.OracleDriver"
		factory="org.apache.tomcat.dbcp.dbcp2.BasicDataSourceFactory"
		username ="test123"
		password="123456"	
		url="jdbc:oracle:thin:@localhost:1521:xe"
		maxTotal="50"
		maxIdle="50"
	/>
</Context>
<!-- 
	- name : 이름은 임의로 지정할 수 있으며,
			이 이름을 통해서 DBCP에 접근, 이용할 수 있게 됨
	- type : "javax.sql.DataSource"로 고정
	- driverClassName : DB Driver
	- factory : dbcp을 관리해주는 JDNI 팩토리
	- 서비스에서 제공하는 데이터 및 객체를 발견(discover)하고
		참고(lookup)하기 위한 자바 API
	-username : DB 접속 ID
	-password : DB 접속 PW
	- uri : DB 주소
	- maxTotal : 동시에 접속할 수 있는 커넥션 객체의 최대 수
	- maxIdle : 커넥션 풀에 반납할때 최대로 유지될수 있는 커넥션의 갯수
	- Oracle.jdbc.driver.OracleDriver 보통은 oracle.jdbc.driver.OracleDriver 가 표준이라, 오류가 발생할수도 있음.

 -->
```

# AppleDAO.java

```java
package com.beaver.may111.apple;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.util.ArrayList;

import javax.servlet.http.HttpServletRequest;

import com.beaver.db.manager.BeaverDBManager;

public class AppleDAO {
	private static final AppleDAO APPLEDAO = new AppleDAO();
	private int allAppleCount;
	public static final AppleDAO getAppledao() {
		return APPLEDAO;
	}
	
	// 모든 사과 데이터 다 가져오기 
	public void getAllApples(HttpServletRequest req) {
		Connection con = null;
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		
		try {
			con = BeaverDBManager.connect("beaverPool");
			// SQL 문 작성
			String sql = "select * from menu order by menu_price";
			pstmt = con.prepareStatement(sql);
			
			// C / U / D : pstmt.executeUpdate()로 실행
			//				=> 데이터 몇 개 영향을 받았는지 
			// R : pstmt.executeQuery()로 실행
			//		=>select 결과를 ResultSet으로 보냄
			rs = pstmt.executeQuery();
			
			ArrayList<Apple> apples = new ArrayList<Apple>();
			Apple apple = null;
			while (rs.next()) {
				apple = new Apple();
				apple.setMenu_location(rs.getString("menu_location"));
				apple.setMenu_colour(rs.getString("menu_colour"));
				apple.setMenu_taste(rs.getString("menu_taste"));
				apple.setMenu_price(rs.getInt("menu_price"));
				apple.setMenu_desc(rs.getString("menu_desc"));
				apples.add(apple);
			}
			req.setAttribute("apples", apples);
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
		}
		BeaverDBManager.close(con, pstmt, rs);
	}
	
	
	// 등록하기
	public void reg(HttpServletRequest req) {
		Connection con = null;
		PreparedStatement pstmt = null;
		try {
			con = BeaverDBManager.connect("beaverPool");
			
			// 데이터 입력
			req.setCharacterEncoding("UTF-8");
			String location = req.getParameter("menu_location");
			String colour = req.getParameter("menu_colour");
			String taste = req.getParameter("menu_taste");
			int price = Integer.parseInt(req.getParameter("menu_price"));
			
			// <textarea>에서 엔터친거 : \r\n
			// 웹 페이지에서 줄 바꾸기 : <br>
			String desc = req.getParameter("menu_desc").replace("\r\n","<br>");
			
			
			// sql문 작성
			String sql = "insert into menu values(?, ?, ?, ?, ?)";
			
			// DB 관련 총괄매니저 (pstmt)
			pstmt = con.prepareStatement(sql);
			
			// sql 완성
			pstmt.setString(1, location);
			pstmt.setString(2, colour);
			pstmt.setString(3, taste);
			pstmt.setInt(4, price);
			pstmt.setString(5, desc);
			
			// 실행 + 결과 처리
			if (pstmt.executeUpdate() == 1) {
				req.setAttribute("r", "[등록성공]");
				allAppleCount++;
				
			}
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
			req.setAttribute("r", "[등록실패]");
//			allAppleCount++;
		}
		BeaverDBManager.close(con, pstmt, null);
	}
	
	// 총 데이터가 몇 개 인지
	public void countApples() {
		Connection con = null;
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		try {
			con = BeaverDBManager.connect("beaverPool");
			String sql = "select count(*) from menu";
			pstmt = con.prepareStatement(sql);
			rs = pstmt.executeQuery();
			rs.next();
			allAppleCount = rs.getInt("count(*)");
			System.out.println(allAppleCount);
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
		}
		BeaverDBManager.close(con, pstmt, rs);
	}
	
	// DB 에 데이터가 100개 있다 ? = > 해당 페이지 번호에 맞는 데이터만 가져오기
	public void getApples(int pageNo, HttpServletRequest req) {
		Connection con = null;
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		try {
			con = BeaverDBManager.connect("beaverPool");
			
			// SQL 수행하는데 필요한 값 챙기기 
			
			// allAppleCount : 전체 사과 데이터 수
			int applePerPage = 4; // 한 페이지당 보여줄 사과 데이터 수
			
			// 페이지 갯수 가져오기
			// Math.ceil : 숫자  올림 (반올림 X)
			int pageCount = (int) Math.ceil(allAppleCount / (double) applePerPage);
			req.setAttribute("pageCount", pageCount);
			///////////////////////////////////////////
			
			int start = (applePerPage * (pageNo - 1)) + 1;
			int end = (pageNo == pageCount) ? allAppleCount : (start + applePerPage - 1);
			
			String sql = "select * from (select rownum as rn, menu_location, menu_colour, menu_taste, menu_price, menu_desc from (select * from menu order by menu_price)) where rn >= ? and rn <= ?";
			
			pstmt = con.prepareStatement(sql);
			pstmt.setInt(1,  start);
			pstmt.setInt(2,  end);
			rs = pstmt.executeQuery();
			
			ArrayList<Apple> apples = new ArrayList<Apple>();
			Apple apple = null;
			while (rs.next()) {
				apple = new Apple();
				apple.setMenu_location(rs.getString("menu_location"));
				apple.setMenu_colour(rs.getString("menu_colour"));
				apple.setMenu_taste(rs.getString("menu_taste"));
				apple.setMenu_price(rs.getInt("menu_price"));
				apple.setMenu_desc(rs.getString("menu_desc"));
				apples.add(apple);
				
			}
			req.setAttribute("apples", apples);
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
		}
		BeaverDBManager.close(con, pstmt, rs);
	}
	
	// 상세정보 가져오기 (Primary Key)
	// 제대로 데이터를 가져왔으면 : true
	// 실패했으면 : false
	public boolean getAppleDetail(HttpServletRequest req) {
		Connection con = null;
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		try {
			con = BeaverDBManager.connect("beaverPool");
			
			String location = req.getParameter("menu_location");
			String sql = "select * from menu where menu_location =?";
			pstmt = con.prepareStatement(sql);
			pstmt.setString(1, location);
			rs = pstmt.executeQuery();
			
			if (rs.next()) {
				Apple apple = new Apple();
				apple.setMenu_location(rs.getString("menu_location"));
				apple.setMenu_colour(rs.getString("menu_colour"));
				apple.setMenu_taste(rs.getString("menu_taste"));
				apple.setMenu_price(rs.getInt("menu_price"));
				apple.setMenu_desc(rs.getString("menu_desc"));
				req.setAttribute("apple", apple);
				return true; // 일반적인 상황 : 데이터가 하나
			}
			return false; // ex) 다른 사람이 그 데이터를 지웠으면
							// : 데이터 없음
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
			return false; // DB 서버 상태가 안좋음(서버 터짐)
		} finally {
			BeaverDBManager.close(con, pstmt, rs);
		}
	}
	
    public boolean update(HttpServletRequest req) {
    	Connection con = null;
    	PreparedStatement pstmt = null;
    	try {
			con = BeaverDBManager.connect("beaverPool");
			req.setCharacterEncoding("UTF-8");
			String location = req.getParameter("menu_location");
			String colour = req.getParameter("menu_colour");
			String taste = req.getParameter("menu_taste");
			int price = Integer.parseInt(req.getParameter("menu_price"));
			String desc = req.getParameter("menu_desc");
			String sql = "update menu set menu_colour=?, menu_taste=?, menu_price=?, menu_desc=? where menu_location=?";
			
			pstmt = con.prepareStatement(sql);
			pstmt.setString(1, colour);
			pstmt.setString(2, taste);
			pstmt.setInt(3, price);
			pstmt.setString(4, desc);
			pstmt.setString(5, location);
			
			if (pstmt.executeUpdate() == 1) {
				req.setAttribute("r", "[수정 성공]");
				return true;
			} else {
				req.setAttribute("r", "[수정 실패]");
				return false;
			}
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
			req.setAttribute("r", "[수정 실패]");
			return false;
		} finally {
			BeaverDBManager.close(con, pstmt, null);
		}
    }
    
    // 데이터 삭제
    public void delete(HttpServletRequest req) {
    	Connection con = null;
    	PreparedStatement pstmt = null;
    	try {
			con = BeaverDBManager.connect("beaverPool");
			String location = req.getParameter("menu_location");
			String sql = "delete from menu where menu_location = ?";
			pstmt = con.prepareStatement(sql);
			pstmt.setString(1, location);
			
			if (pstmt.executeUpdate() == 1) {
				req.setAttribute("r", "[삭제 성공]");
				allAppleCount--;
			} else {
				req.setAttribute("r", "[삭제 실패]");
			}
			
		} catch (Exception e) {
			// TODO: handle exception
			e.printStackTrace();
			req.setAttribute("r", "[삭제 실패]");
		}
    	BeaverDBManager.close(con, pstmt, null);
    }

}

```

# DBCController.java

```java
package com.beaver.may111.dbc;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/DBCController")
public class DBCController extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    public DBCController() {
        super();
        // TODO Auto-generated constructor stub
    }

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		request.setAttribute("cp",  "menu04.jsp");
		request.getRequestDispatcher("index.jsp").forward(request, response);

	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {

	}

}

```

#
