# 예제

## p01_HTTP_OracleDB

```python
'''
Created on 2026. 6. 15.

@author: sdedu01
'''

#http://openAPI.seoul.go.kr:8088/4f626857416f6c6c3632586a416843/xml/TimeAverageAirQuality/1/24/20260614/강남구
#4f626857416f6c6c3632586a416843
#연월일, 시간대, 이산화질소, 오존, 일산화탄소, 아황산가스, 
#    미세먼저, 초미세먼지의 정보를 DB에 저장

import requests
import oracledb
from oracledb import connection
import xml.etree.ElementTree as ET
from http.client import HTTPConnection
from xml.etree.ElementTree import fromstring
from urllib.parse import quote
'''
url = "http://openAPI.seoul.go.kr:8088/4f626857416f6c6c3632586a416843/xml/TimeAverageAirQuality/1/24/20260614/강남구"
# response = requests.get(url)
DATE = "20260614"
if response.status_code == 200:
    root = ET.fromstring(response.text)
    rows = root.findall("row")
else:
    print(f"호출실패: {response.status_code}")
    
dsn_string="localhost:1521:xe"
connection = None

try:
    connection = oracledb.connect(
            user="test123",
            password="123456",
            dsn=dsn_string
        )
    cursor = connection.cursor()
    
    sql = """
        insert into api01 (
                            MSRMT_DT, 
                            MSRSTN_NM, 
                            NTDX, 
                            OZON,
                            CBMX, 
                            SPDX, 
                            PM, 
                            FPM
                        ) values (
                            TO_DATE(:MSRMT_DT, 'YYYYMMDDHH24MI'),
                            :MSRMT_DT, 
                            :MSRSTN_NM, 
                            :NTDX, 
                            :OZON,
                            :CBMX, 
                            :SPDX, 
                            :PM, 
                            :FPM
    """
    
    for item in rows:
        data_to_insert = (
            item.get("MSRMT_DT"),
            item.get("MSRSTN_NM"),
            item.get("NTDX"),
            item.get("OZON"),
            item.get("CBMX"),
            item.get("SPDX"),
            item.get("PM"),
            item.get("FPM")
            
        )
        cursor.execute(sql, data_to_insert)
    connection.commit()
    print("DB에 저장됨.")
except Exception as e:
    print(f"DB 에러 발생: {e}")
    
finally:
    if 'cursor' in locals():
        cursor.close()
    if 'connection' in locals():
        connection.close()
        print("DB 연결 종료")
'''

oracledb.init_oracle_client(
        lib_dir="C:\\Users\\sdedu01\\Documents\\instantclient_21_19"

    )

dsn_string = "localhost:1521/xe"

con = connection = oracledb.connect(

        user="test123",
        password="123456",
        dsn=dsn_string
    )
'''
hc = HTTPConnection("openapi.seoul.go.kr:8088")
hc.request("GET", "/4f626857416f6c6c3632586a416843/xml/TimeAverageAirQuality/1/24/20260614/강남구")
resBody = hc.getresponse().read()
# print(resBody.decode())
'''
hc = HTTPConnection("openapi.seoul.go.kr", 8088)

gu = quote("강남구", encoding="utf-8")
url = f"/4f626857416f6c6c3632586a416843/xml/TimeAverageAirQuality/1/24/20260614/{gu}"

hc.request("GET", url)
resBody = hc.getresponse().read()

cur = con.cursor()
for a in fromstring(resBody).iter("row"):
    sql = "insert into jun15_airqual values("
    sql += "jun15_airqual_seq.nextval, "
    sql += f"'{a.find('MSRMT_DT').text[0:8]}', "
    sql += f"'{a.find('MSRMT_DT').text[8:10]}', "
    sql += f"'{a.find('NTDX').text}', "
    sql += f"'{a.find('OZON').text}', "
    sql += f"'{a.find('CBMX').text}', "
    sql += f"'{a.find('SPDX').text}', "
    sql += f"'{a.find('PM').text}', "
    sql += f"'{a.find('FPM').text}')"
    cur.execute(sql)
    
con.commit()
con.close()
print("종료!")
        

```

---

## p02_MakeCSV

```python
'''
Created on 2026. 6. 15.

@author: sdedu01
'''
import oracledb 
import csv

'''
csv 파일로 만들어주세요!
연월일, 시간대, 이산화질소, 오존, 일산화탄소, 아황산가스, 
    미세먼저, 초미세먼지의 형태로 (DB -> CSV)
'''

# username = "test123"
# password = "123456"
# dsn = "localhost:1521/xe"

# try:
oracledb.init_oracle_client(
        lib_dir="C:\\Users\\sdedu01\\Documents\\instantclient_21_19"

    )

con = connection = oracledb.connect(

    user="test123",
    password="123456",
    dsn = "localhost:1521/xe"
    )

f = open("C:\\Users\\sdedu01\\Desktop\\Test\\gn.csv",
         "a", encoding="UTF-8")

query = "select * from JUN15_AIRQUAL"
# df = pandas.read_sql(query)

cur = con.cursor()
cur.execute(query)

for _, date, time, ntdx, ozon, cbmx, spdx, pm, fpm in cur:
    f.write(f"{date}, {time}, {ntdx}, {ozon}, {cbmx},{spdx}, {pm},{fpm}\n")
    
# df.to_csv('gn.csv', index=False, encoding='utf-8-sig')
f.close()
con.close()
print('추출 완료!')
# except Exception as e:
#     print(f'오류 발생: {e}')
# finally:
    

```

---

## p03_seoulAir

```python
'''
Created on 2026. 6. 15.

@author: sdedu01

http://openAPI.seoul.go.kr:8088/4f626857416f6c6c3632586a416843/json/RealtimeCityAir/1/25/
4f626857416f6c6c3632586a416843

측정소명, 미세먼지, 초미세먼지 의 정보를 DB에 저장
    (여러 기간에 걸쳐 이 데이터를 모은다고 가정)
'''

import requests
import oracledb
from oracledb import connection
from http.client import HTTPConnection
import json

oracledb.init_oracle_client(
        lib_dir="C:\\Users\\sdedu01\\Documents\\instantclient_21_19"

    )

dsn_string = "localhost:1521/xe"

con = connection = oracledb.connect(

        user="test123",
        password="123456",
        dsn=dsn_string
    )

hc = HTTPConnection('openapi.seoul.go.kr:8088')
# url=f"/4f626857416f6c6c3632586a416843/json/RealtimeCityAir/1/25/"
# url="http://openAPI.seoul.go.kr:8088/4f626857416f6c6c3632586a416843/json/RealtimeCityAir/1/25/"
hc.request("GET", "/4f626857416f6c6c3632586a416843/json/RealtimeCityAir/1/25/")
resBody = hc.getresponse().read()
# print(resBody.decode())
data = json.loads(resBody)

cur = con.cursor()
for a in data["RealtimeCityAir"]["row"]:
    quote = "insert into seoul values("
    quote += "seoul_seq.nextval, "
    quote += f"'{a['MSRSTN_NM']}',{a['PM']},{a['FPM']})"
    cur.execute(quote)

'''
for a in fromstring(resBody).iter("row"):
    quote = "insert into jun15_seoul values("
    quote += "jun15_seoul_seq.nextval, "
    quote += f"'{a.find('MSRMT_DT').text[0:8]}', "
    quote += f"'{a.find('MSRMT_DT').text[8:10]}', "
    quote += f"'{a.find('MSRSTN_NM').text}', "
    quote += f"'{a.find('PM').text}', "
    quote += f"'{a.find('FPM').text}', "
    # quote += f"'{a.find('CBMX').text}', "
    # quote += f"'{a.find('SPDX').text}', "
    # quote += f"'{a.find('PM').text}', "
    # quote += f"'{a.find('FPM').text}')"
    cur.execute(quote)
quote = """
    insert into seoul
    values (
        seoul_seq.nextval,
        :1,
        :2,
        :3,
        :4,
        :5
    )
"""
rows = data["RealtimeCityAir"]["row"]
for a in rows:
    msrmt_dt = a["MSRMT_DT"]

    cur.execute(quote, (
        quote += f"'{a.find('MSRMT_DT').text[0:8]}', "
        quote += f"'{a.find('MSRMT_DT').text[8:10]}', "
        a["MSRSTN_NM"],
        a["PM"],
        a["FPM"]
    ))
'''
con.commit()
con.close()
print("종료!")

```

---

## p04_seoulAir2

```python
'''
Created on 2026. 6. 15.

@author: sdedu01

DB에 있는 미세먼지 데이터
    => CSV에 저장 (데이터들은 ,로 구분)
'''

import csv
import oracledb

oracledb.init_oracle_client(
        lib_dir="C:\\Users\\sdedu01\\Documents\\instantclient_21_19"

    )

con = connection = oracledb.connect(

    user="test123",
    password="123456",
    dsn = "localhost:1521/xe"
    )

f = open("C:\\Users\\sdedu01\\Desktop\\Test\\seoulAir2.csv",
         "a", encoding="UTF-8")

query = "select * from seoul"

cur = con.cursor()
cur.execute(query)

for number,msrmt_nm, pm, fpm in cur:
    f.write(f"{number}, {msrmt_nm}, {pm}, {fpm}\n")
    
f.close()
con.close()
print('추출 완료!')

```

---

## db.sql

```sql
create table jun15_airqual(
	aq_no number(4) primary key,
	aq_date varchar2(10 char) not null,
	aq_time varchar2(100 char) not null,
	aq_ntdx varchar2(100 char) not null,
	aq_ozon varchar2(100 char) not null,
	aq_cbmx varchar2(100 char) not null,
	aq_spdx varchar2(100 char) not null,
	aq_pm varchar2(100 char) not null,
	aq_fpm varchar2(100 char) not null

);
create sequence jun15_airqual_seq;
select * from JUN15_AIRQUAL;

drop table seoul;
drop sequence seoul_seq;
create table seoul(
	s_no number(4) primary key,
	s_MSRMT_NM varchar2(100 char) not null,
	s_pm number(4,1) not null,
	s_fpm number(4,1) not null
);
create sequence seoul_seq;
select * from seoul;
```

---

## p05_final

<aside>
💡

.py

*메뉴 만들기 (함수)*

- *1. 학생 등록 / 2.강의장 조회 /3. 학생 조회*
- *4. 학생정보를 파일로 내보내기 / 0. 종료*

*학생 등록 (정보 입력해서)*

- *강의장 조회 : ex)1강의장 - 5층 복도 오른쪽*
- *학생 조회 : 이름, 생년월일(연-월-일(요일)), 나이, 몇 강의장, 중간, 기말, 평균 점수*
- *파일로 내보내기 (학생의 전체 정보를 CSV 파일로)*

---

.sql

- (table간 제약조건 고려 X)
- 학생 : 이름, 생일, 몇 강의장, 중간, 기말, 평균 점수

</aside>

- 

### db.sql

```sql
-- (table간 제약조건 고려 X)
-- 학생 : 이름, 생일, 몇 강의장, 중간, 기말, 평균 점수

DROP TABLE STUDENT;
DROP SEQUENCE STUDENT_SEQ;
CREATE TABLE student(
	st_no number(4) primary key,
	st_name varchar2(100 char) not null,
	st_birth varchar2(100 char) not null,
	st_classroom varchar2(500 char) not null,
	st_mExam varchar2(500 char) not null,
	st_fExam varchar2(500 char) not null,
	st_score varchar2(1500 char) not null
);
CREATE SEQUENCE student_seq;
SELECT * FROM STUDENT;

INSERT INTO STUDENT VALUES(
	student_seq.NEXTVAL,
	'이재명',
	'19641222',
	'1강의장',
	'65',
	'75',
	'70'
);

-- 강의장 : 몇 강의장, 강의장 위치
DROP TABLE CLASSROOM;
DROP SEQUENCE classroom_seq;
CREATE TABLE classroom(

	cl_name varchar2(500 char) primary key,
	cl_loca varchar2(1000 char) not null
);
CREATE SEQUENCE classroom_seq;
SELECT * FROM CLASSROOM;

-- 1강의장 : 5층 복도 오른쪽 / 2강의장 : 5층 복도 왼쪽 끝
INSERT INTO classroom VALUES(

	'1강의장',
	'5층 복도 오른쪽'
);
INSERT INTO CLASSROOM values(

	'2강의장',
	'5층 복도 왼쪽 끝'
);
-- 3강의장 : 5층 복도 왼쪽 첫번째
INSERT INTO CLASSROOM values(

	'3강의장',
	'5층 복도 왼쪽 첫번째'
);
-- 4강의장 : 6층 왼쪽 / 5강의장 : 6층 오른쪽
INSERT INTO CLASSROOM values(

	'4강의장',
	'6층 왼쪽'
);
INSERT INTO CLASSROOM values(

	'5강의장',
	'6층 오른쪽'
);

UPDATE student
SET st_birth = '19530124'
WHERE st_name = '문재인';

COMMIT;

SELECT COUNT(*) FROM CLASSROOM;
```

---

### p05_final

```python
'''
Created on 2026. 6. 15.

@author: sdedu01

메뉴 만들기 (함수)
1. 학생 등록 / 2.강의장 조회 /3. 학생 조회
    4. 학생정보를 파일로 내보내기 / 0. 종료
    
학생 등록 (정보 입력해서)
강의장 조회 : ex)1강의장 - 5층 복도 오른쪽
학생 조회 : 이름, 생년월일(연-월-일(요일)), 나이,
                몇 강의장, 중간, 기말, 평균 점수

파일로 내보내기 (학생의 전체 정보를 CSV 파일로)
'''
from _datetime import datetime
import requests
import oracledb

students = []

        
oracledb.init_oracle_client(
        lib_dir="C:\\Users\\sdedu01\\Documents\\instantclient_21_19"

    )

con = connection = oracledb.connect(

    user="test123",
    password="123456",
    dsn = "localhost:1521/xe"
    )

def showMenu():
    print("1.학생등록")
    print("2.강의장 조회 ")
    print("3.학생 조회")
    print("4.학생정보를 파일로 내보내기")
    print("0.종료")
    return input("번호 입력: ")
    
def registerStudent():
    print("\n[학생등록]")
    name = input("이름:")
    birth = input("생년월일:")
    classroom = input("강의장:")
    mExam = input("중간고사 점수:") # mExam = int(input("중간고사 점수:"))
    fExam = input("기말고사 점수:") # fExam = int(input("기말고사 점수:"))
    

    birth1 = datetime.strptime(birth, "%Y%m%d")
    birth_str = birth1.strftime("%Y-%m-%d(%a)")

    today = datetime.today()
    age = today.year - birth1.year
    
    mExam_calc = float(mExam)
    fExam_calc = float(fExam)
    avg = (mExam_calc + fExam_calc) / 2.0
    
    cur = con.cursor()
    query = """INSERT INTO STUDENT (st_no, st_name, st_birth, st_classroom, st_mExam, st_fExam, st_score) VALUES (student_seq.NEXTVAL, :name, :birth, :classroom, :mExam, :fExam, :score)"""

    cur.execute(query, name=name, birth=birth, classroom=classroom, mExam=mExam_calc, fExam=fExam_calc, score=str(avg))
    if cur.rowcount == 1:
        print("등록 성공!")
        con.commit()
    
    cur.close()
    
    print("등록 완료!")
    
    #####################################################

        
def showCR():

    sql = "SELECT cl_name, cl_loca FROM CLASSROOM ORDER BY cl_name"
    cur = con.cursor()
    cur.execute(sql)
    count = 0
    for name, loca in cur:
        print(f"{name} - {loca}")
        count += 1
    if count == 0:
        print("등록된 강의장이 없음.")
        
    cur.close()
    
def showStudent():
    
    sql = "select st_name, st_birth, st_classroom, st_mExam, st_fExam, st_score from student"
    cur = con.cursor()
    cur.execute(sql)
    now = datetime.today()
    
    for name, birth, classroom, mExam, fExam, score in cur:
        if "-" in birth:
            birth_date = datetime.strptime(birth, "%Y-%m-%d(%a)")
        else:
            birth_date = datetime.strptime(birth, "%Y%m%d")
        
        
        mExam = float(mExam)
        fExam = float(fExam)
        avg = (mExam + fExam) / 2
        
        print("##########")
        print(f"이름:{name}")
        print(f"생일:{birth_date.strftime('%Y-%m-%d (%a)')}")
        print(f"나이:{now.year - birth_date.year + 1}세")
        print(f"강의장:{classroom}")
        print(f"중간고사 점수:{mExam}점")
        print(f"기말고사 점수:{fExam}점")
        print(f"평균 점수:{avg}점")
        print("##########")
        
    cur.close()
    
def saveFile():
    
    f = open("C:\\Users\\sdedu01\\Desktop\\Test\\StudentInfo.csv", "w", encoding="utf-8")
    
    sql = "select * from student"
    cur = con.cursor()
    cur.execute(sql)
    for no, name, birth, classroom, mExam, fExam, score in cur:
        f.write(f"{no}, {name}, {birth}, {classroom}, {mExam}, {fExam}, {score}\n")
    f.close()
    
    
    cur.close()
    print("파일 작성 성공!")

        
def start():        
    while True:
        print("1. 학생 등록")
        print("2. 강의장 조회")
        print("3. 학생 조회")
        print("4. 학생정보를 파일로 내보내기")
        print("0. 종료")
    
        choice = input("선택하세요:")
    
        if choice == '1':
            registerStudent()
    
        elif choice == '2':
            showCR()
    
        elif choice == '3':
            showStudent()
            
        elif choice == '4':
            saveFile()
            
        elif choice == '0':
            con.close()
            print("종료합니다.")
            break;
        else:
            print("잘못된 입력임, 다시 선택 요망.")
 
       
start()

```

가끔 오류로 인해, 일부 DB 데이터를 못 불러오기도 한다.

그럴때는,이클립스를 재부팅해야만 함.
